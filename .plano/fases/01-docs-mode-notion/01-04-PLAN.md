---
phase: 01-docs-mode-notion
plan: 01-04
type: feature
autonomous: true
wave: 3
depends_on: [01-01, 01-02, 01-03]
requirements: [REQ-04]
files_modified:
  - src/pages/Page.tsx
  - src/App.tsx
  - src/hooks/useSupabaseData.ts
  - src/hooks/useCrudMutations.ts
  - src/components/page/PageHeader.tsx
  - src/components/page/usePageAutoSave.ts
  - src/components/workspace/TrashDrawer.tsx
must_haves:
  truths:
    - "Rota `/page/:pageId` existe e e protegida (ProtectedRoute), monta page Page.tsx"
    - "Page.tsx carrega `usePage(pageId)` (titulo + content), renderiza PageHeader + PageEditor"
    - "Editar titulo no PageHeader chama useRenamePage com debounce de 500ms"
    - "Editar conteudo do editor chama useUpdatePageContent com debounce de 1500ms"
    - "Indicador 'Salvando.../Salvo' aparece no PageHeader durante o save (reusa padrao SavingIndicator)"
    - "Botao 'Excluir' move page para trash (state='deleted') e navega de volta para `/`"
    - "TrashDrawer mostra pages deletadas ao lado de boards e items deletados"
    - "Restore de page volta state='active' e re-aparece no sidebar"
    - "Acesso negado (RLS) retorna pagina 'Sem permissao' (nao crash)"
  artifacts:
    - path: "src/pages/Page.tsx"
      provides: "Pagina-raiz da rota /page/:pageId. Le params, monta header + editor + auto-save"
    - path: "src/components/page/usePageAutoSave.ts"
      provides: "Hook que recebe pageId + blocks e faz upsert debounced em pages.content"
    - path: "src/components/page/PageHeader.tsx"
      provides: "Header com titulo editavel, indicador de save, menu (Renomear/Excluir/Historico/Permissoes)"
    - path: "src/hooks/useSupabaseData.ts"
      provides: "usePage(pageId) — query da page individual"
    - path: "src/hooks/useCrudMutations.ts"
      provides: "useUpdatePageContent, useRestorePage"
  key_links:
    - from: "Page.tsx"
      to: "usePageAutoSave"
      via: "passa onChange callback do PageEditor ao hook que debouncea e chama useUpdatePageContent"
    - from: "PageHeader"
      to: "useRenamePage"
      via: "onBlur do input de titulo dispara mutate"
    - from: "Page.tsx"
      to: "supabase pages.content jsonb"
      via: "auto-save via mutation useUpdatePageContent"
---

# Fase 01 Plano 01-04: CRUD de pagina com auto-save debounced

**Objetivo:** Tornar a pagina utilizavel ponta-a-ponta. Rota dedicada, editor montado com conteudo do banco, auto-save invisivel, lixeira/restauracao funcionando. Apos este plano um usuario consegue criar, editar, abandonar, voltar e recuperar uma pagina.

## Achados de Pesquisa (Inline)

- `setTimeout` + clearTimeout dentro de `useEffect` e o padrao do BlockNote para auto-save (oficial: `onChange` -> debounce timer -> mutation).
- BlockNote `editor.document` ja retorna `Block[]` serializavel direto pra JSON. NAO precisa `editor.blocksToJSON()` (esse metodo NEM EXISTE; a interface ja e JSON-friendly).
- Padrao do projeto para "trash":
  - `useTrash.ts` ja lista items + boards deletados. Vamos estender para pages.
  - `TrashDrawer.tsx` tem tabs Items / Boards. Adicionar tab Pages.

## Contexto

@src/App.tsx — registro de rotas, ProtectedRoute
@src/components/page/PageEditor.tsx — editor base (criado em 01-02)
@src/hooks/useCrudMutations.ts — useCreatePage/useDeletePage/useRenamePage (criados em 01-03)
@src/components/board/SavingIndicator.tsx — padrao visual de "salvando..."
@src/hooks/useTrash.ts — hook de lixeira (estender)
@src/components/workspace/TrashDrawer.tsx — drawer de lixeira (estender)

## Tarefas

<task id="1" type="auto">
<files>src/hooks/useSupabaseData.ts</files>
<action>
Adicionar `usePage(pageId)` na secao Pages (depois de `useWorkspaceEntries`).

```typescript
export const usePage = (pageId?: string | null) =>
  useQuery({
    queryKey: ['page', pageId],
    enabled: !!pageId,
    staleTime: 30 * 1000,
    queryFn: async () => {
      const { data, error } = await supabase
        .from('pages')
        .select('id, workspace_id, folder_id, title, content, state, icon, cover_url, position, created_by, created_at, updated_at')
        .eq('id', pageId!)
        .single();
      if (error) throw error;
      return data;
    },
  });
```

Retorno e single (nao array). Tipo do `content` vem como `Json` do Supabase; consumidor cast para `PartialBlock[]` quando passar ao PageEditor.
</action>
<verify>
  <automated>cd /home/projects/hub-tarefas-lfpro && grep -E "usePage\b" src/hooks/useSupabaseData.ts | head -3</automated>
</verify>
<done>Hook exportado, query key `['page', pageId]`.</done>
</task>

<task id="2" type="auto">
<files>src/hooks/useCrudMutations.ts</files>
<action>
Adicionar duas mutations apos as criadas em 01-03:

```typescript
export const useUpdatePageContent = () => {
  const qc = useQueryClient();
  return useMutation({
    retry: 1,
    mutationFn: async ({
      pageId,
      content,
    }: {
      pageId: string;
      content: unknown[]; // BlockNote Block[]
    }) => {
      const { error } = await supabase
        .from('pages')
        .update({ content })
        .eq('id', pageId);
      if (error) throw error;
    },
    onSuccess: (_data, vars) => {
      qc.invalidateQueries({ queryKey: ['page', vars.pageId] });
      // NAO invalidar ['pages'] inteiro para evitar refetch do sidebar a cada save
    },
  });
};

export const useRestorePage = () => {
  const qc = useQueryClient();
  return useMutation({
    retry: 1,
    mutationFn: async (id: string) => {
      const { error } = await supabase
        .from('pages')
        .update({ state: 'active' })
        .eq('id', id);
      if (error) throw error;
    },
    onSuccess: () => {
      qc.invalidateQueries({ queryKey: ['pages'] });
      qc.invalidateQueries({ queryKey: ['all-pages'] });
      qc.invalidateQueries({ queryKey: ['trash-pages'] });
    },
  });
};
```

Padroes: retry: 1, sem `any`, sem em-dash em comentarios.
</action>
<verify>
  <automated>cd /home/projects/hub-tarefas-lfpro && grep -E "useUpdatePageContent|useRestorePage" src/hooks/useCrudMutations.ts | wc -l</automated>
</verify>
<done>Mutations exportadas. Tipos OK.</done>
</task>

<task id="3" type="auto">
<files>src/components/page/usePageAutoSave.ts</files>
<action>
Criar hook de auto-save com debounce 1500ms. Padrao timer + clearTimeout para garantir cancelamento em unmount.

Conteudo literal:

```typescript
import { useCallback, useEffect, useRef, useState } from 'react';
import { useUpdatePageContent } from '@/hooks/useCrudMutations';

export type AutoSaveStatus = 'idle' | 'pending' | 'saving' | 'saved' | 'error';

interface UsePageAutoSaveOptions {
  pageId: string;
  debounceMs?: number;
}

/**
 * Hook de auto-save para conteudo de pagina.
 * Acumula um Block[] pendente e dispara mutation 1.5s apos ultima mudanca.
 * Retorna status para o header mostrar "Salvando..." / "Salvo".
 */
export function usePageAutoSave({ pageId, debounceMs = 1500 }: UsePageAutoSaveOptions) {
  const mutation = useUpdatePageContent();
  const timerRef = useRef<ReturnType<typeof setTimeout> | null>(null);
  const pendingRef = useRef<unknown[] | null>(null);
  const [status, setStatus] = useState<AutoSaveStatus>('idle');

  const flush = useCallback(async () => {
    if (!pendingRef.current) return;
    const content = pendingRef.current;
    pendingRef.current = null;
    setStatus('saving');
    try {
      await mutation.mutateAsync({ pageId, content });
      setStatus('saved');
      // Volta para idle apos 2s
      setTimeout(() => {
        setStatus((s) => (s === 'saved' ? 'idle' : s));
      }, 2000);
    } catch {
      setStatus('error');
    }
  }, [mutation, pageId]);

  const schedule = useCallback(
    (blocks: unknown[]) => {
      pendingRef.current = blocks;
      setStatus('pending');
      if (timerRef.current) clearTimeout(timerRef.current);
      timerRef.current = setTimeout(() => {
        timerRef.current = null;
        void flush();
      }, debounceMs);
    },
    [flush, debounceMs]
  );

  // Flush ao desmontar (so se houver pendente)
  useEffect(() => {
    return () => {
      if (timerRef.current) {
        clearTimeout(timerRef.current);
      }
      if (pendingRef.current) {
        void flush();
      }
    };
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);

  return { schedule, status, flush };
}
```

Notas:
- NAO usar em-dash em comentarios.
- `void flush()` para silenciar Promise no useEffect cleanup.
- Sem `any`. `unknown[]` aceita o `Block[]` do BlockNote.
</action>
<verify>
  <automated>cd /home/projects/hub-tarefas-lfpro && npx tsc --noEmit src/components/page/usePageAutoSave.ts 2>&1 | grep "error" | head -3 || echo "OK"</automated>
</verify>
<done>Hook criado, compila sem erros, exporta `usePageAutoSave` e `AutoSaveStatus`.</done>
</task>

<task id="4" type="auto">
<files>src/components/page/PageHeader.tsx</files>
<action>
Componente PageHeader com titulo editavel, indicador de save, menu (3 dots).

Conteudo literal:

```tsx
import React, { useState, useEffect, useRef } from 'react';
import { useNavigate } from 'react-router-dom';
import { MoreHorizontal, History, Trash2, Shield } from 'lucide-react';
import { Button } from '@/components/ui/button';
import {
  DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuTrigger, DropdownMenuSeparator,
} from '@/components/ui/dropdown-menu';
import { useRenamePage, useDeletePage } from '@/hooks/useCrudMutations';
import { toast } from 'sonner';
import type { AutoSaveStatus } from './usePageAutoSave';

interface Props {
  pageId: string;
  initialTitle: string;
  saveStatus: AutoSaveStatus;
  onOpenHistory?: () => void;
  onOpenPermissions?: () => void;
}

const STATUS_LABEL: Record<AutoSaveStatus, string> = {
  idle: '',
  pending: 'Editando...',
  saving: 'Salvando...',
  saved: 'Salvo',
  error: 'Erro ao salvar',
};

const PageHeader: React.FC<Props> = ({
  pageId,
  initialTitle,
  saveStatus,
  onOpenHistory,
  onOpenPermissions,
}) => {
  const [title, setTitle] = useState(initialTitle);
  const renamePage = useRenamePage();
  const deletePage = useDeletePage();
  const navigate = useNavigate();
  const debounceRef = useRef<ReturnType<typeof setTimeout> | null>(null);

  useEffect(() => {
    setTitle(initialTitle);
  }, [initialTitle]);

  const persistTitle = (next: string) => {
    if (next.trim() === '' || next === initialTitle) return;
    renamePage.mutate(
      { pageId, title: next.trim() },
      {
        onError: () => toast.error('Erro ao renomear pagina'),
      }
    );
  };

  const handleTitleChange = (e: React.ChangeEvent<HTMLInputElement>) => {
    const next = e.target.value;
    setTitle(next);
    if (debounceRef.current) clearTimeout(debounceRef.current);
    debounceRef.current = setTimeout(() => {
      persistTitle(next);
    }, 500);
  };

  const handleDelete = () => {
    if (!confirm('Mover esta pagina para a lixeira?')) return;
    deletePage.mutate(pageId, {
      onSuccess: () => {
        toast.success('Pagina movida para lixeira');
        navigate('/');
      },
      onError: () => toast.error('Erro ao excluir pagina'),
    });
  };

  return (
    <div className="border-b border-border px-6 py-4 flex items-center gap-4 bg-background sticky top-0 z-10">
      <input
        value={title}
        onChange={handleTitleChange}
        onBlur={() => persistTitle(title)}
        placeholder="Pagina sem titulo"
        className="flex-1 bg-transparent border-0 outline-none font-heading text-xl font-bold placeholder:text-muted-foreground/60"
      />
      <span
        className={`text-xs ${
          saveStatus === 'error' ? 'text-destructive' : 'text-muted-foreground'
        }`}
      >
        {STATUS_LABEL[saveStatus]}
      </span>
      <DropdownMenu>
        <DropdownMenuTrigger asChild>
          <Button variant="ghost" size="icon" className="h-8 w-8">
            <MoreHorizontal className="w-4 h-4" />
          </Button>
        </DropdownMenuTrigger>
        <DropdownMenuContent align="end">
          <DropdownMenuItem onClick={onOpenHistory} disabled={!onOpenHistory}>
            <History className="w-3.5 h-3.5 mr-2" />
            Historico de versoes
          </DropdownMenuItem>
          <DropdownMenuItem onClick={onOpenPermissions} disabled={!onOpenPermissions}>
            <Shield className="w-3.5 h-3.5 mr-2" />
            Permissoes
          </DropdownMenuItem>
          <DropdownMenuSeparator />
          <DropdownMenuItem
            onClick={handleDelete}
            className="text-destructive focus:text-destructive"
          >
            <Trash2 className="w-3.5 h-3.5 mr-2" />
            Excluir pagina
          </DropdownMenuItem>
        </DropdownMenuContent>
      </DropdownMenu>
    </div>
  );
};

export default PageHeader;
```

Notas:
- Historico e Permissoes ficam disabled enquanto callbacks nao chegam (plano 01-06 e 01-07 conectam).
- Toast em pt-BR.
- `confirm()` nativo basta no MVP; substituir por AlertDialog do shadcn se quiser polish futuro.
- Sem em-dash.
</action>
<verify>
  <automated>cd /home/projects/hub-tarefas-lfpro && npx tsc --noEmit 2>&1 | grep "PageHeader" | head -3 || echo "OK"</automated>
</verify>
<done>Componente compila. Titulo editavel com debounce 500ms. Menu com Historico/Permissoes/Excluir.</done>
</task>

<task id="5" type="auto">
<files>src/pages/Page.tsx</files>
<action>
Pagina-raiz da rota `/page/:pageId`.

Conteudo literal:

```tsx
import React from 'react';
import { useParams, Navigate } from 'react-router-dom';
import { usePage } from '@/hooks/useSupabaseData';
import PageEditor from '@/components/page/PageEditor';
import PageHeader from '@/components/page/PageHeader';
import { usePageAutoSave } from '@/components/page/usePageAutoSave';
import LoadingScreen from '@/components/shared/LoadingScreen';
import type { PartialBlock } from '@blocknote/core';

const PagePage: React.FC = () => {
  const { pageId } = useParams<{ pageId: string }>();
  const { data: page, isLoading, error } = usePage(pageId);
  const autoSave = usePageAutoSave({ pageId: pageId ?? '' });

  if (!pageId) {
    return <Navigate to="/" replace />;
  }

  if (isLoading) {
    return <LoadingScreen />;
  }

  if (error || !page) {
    return (
      <div className="flex flex-col items-center justify-center h-screen text-center px-6">
        <h2 className="font-heading text-xl font-bold mb-2">Pagina nao encontrada</h2>
        <p className="text-sm text-muted-foreground">
          A pagina nao existe ou voce nao tem permissao para acessa-la.
        </p>
      </div>
    );
  }

  if (page.state !== 'active') {
    return (
      <div className="flex flex-col items-center justify-center h-screen text-center px-6">
        <h2 className="font-heading text-xl font-bold mb-2">Pagina arquivada</h2>
        <p className="text-sm text-muted-foreground">
          Esta pagina foi removida. Restaure-a na lixeira para edita-la.
        </p>
      </div>
    );
  }

  const initialContent = (page.content as PartialBlock[] | null) ?? undefined;

  return (
    <div className="flex flex-col h-screen bg-background">
      <PageHeader
        pageId={page.id}
        initialTitle={page.title}
        saveStatus={autoSave.status}
      />
      <div className="flex-1 overflow-y-auto py-8">
        <PageEditor
          initialContent={initialContent}
          onChange={(blocks) => autoSave.schedule(blocks)}
          editable
        />
      </div>
    </div>
  );
};

export default PagePage;
```

Notas:
- Sem AppSidebar/TopNavBar neste MVP. Pagina ocupa viewport inteiro (foco no editor). Sidebar acessivel se o usuario voltar para `/`.
- DECISAO TOMADA pelo planejamento: nao misturar Page com a estrutura do `Index.tsx` para evitar bagunca dos contexts de Board. Se usuario reclamar de navegacao, plano futuro pode adicionar sidebar embed.
</action>
<verify>
  <automated>cd /home/projects/hub-tarefas-lfpro && npx tsc --noEmit 2>&1 | grep "src/pages/Page" | head -3 || echo "OK"</automated>
</verify>
<done>Pagina monta editor, header e auto-save. Tratamento de loading/erro/arquivada presente.</done>
</task>

<task id="6" type="auto">
<files>src/App.tsx</files>
<action>
Registrar a rota `/page/:pageId` como protegida + lazy.

Mudancas:

1. Adicionar import lazy no topo (proximo aos outros):
```typescript
const PagePage = React.lazy(() => import('./pages/Page'));
```

2. Dentro de `ProtectedApp` (linha ~41), adicionar a rota:
```tsx
<Route path="/page/:pageId" element={<PagePage />} />
```

NAO alterar nada mais. NAO mexer em rotas publicas.
</action>
<verify>
  <automated>cd /home/projects/hub-tarefas-lfpro && grep -E "PagePage|/page/:pageId" src/App.tsx | wc -l</automated>
</verify>
<done>Rota registrada como lazy + protegida. `npm run build` passa.</done>
</task>

<task id="7" type="auto">
<files>src/hooks/useTrash.ts</files>
<action>
Estender `useTrash` (existente) para incluir pages deletadas.

Investigar primeiro:
```bash
grep -n "useTrash\|state.*deleted" src/hooks/useTrash.ts
```

Padrao esperado: hook ja retorna `{ deletedBoards, deletedItems }`. Adicionar `deletedPages`:

```typescript
// Adicionar:
export const useDeletedPages = () =>
  useQuery({
    queryKey: ['trash-pages'],
    staleTime: 30 * 1000,
    queryFn: async () => {
      const { data, error } = await supabase
        .from('pages')
        .select('id, workspace_id, title, icon, updated_at')
        .eq('state', 'deleted')
        .order('updated_at', { ascending: false });
      if (error) throw error;
      return data ?? [];
    },
  });
```

Se `useTrash` for um hook agregador (chama useDeletedBoards, useDeletedItems), adicionar `useDeletedPages` ao mix e expor `deletedPages` no retorno.
</action>
<verify>
  <automated>cd /home/projects/hub-tarefas-lfpro && grep -E "useDeletedPages|trash-pages" src/hooks/useTrash.ts | wc -l</automated>
</verify>
<done>Hook lista pages deletadas. Cache invalidada em useDeletePage/useRestorePage (ja feito).</done>
</task>

<task id="8" type="auto">
<files>src/components/workspace/TrashDrawer.tsx</files>
<action>
Adicionar aba "Paginas" no TrashDrawer.

Investigar layout atual:
```bash
grep -n "Tabs\|TabsList\|TabsTrigger" src/components/workspace/TrashDrawer.tsx | head -10
```

Esperado: ja existe estrutura com Tabs do shadcn. Adicionar `<TabsTrigger value="pages">Paginas</TabsTrigger>` e `<TabsContent value="pages">` com lista mapeando `deletedPages` (cada uma com botao Restaurar que chama `useRestorePage`).

Layout literal do TabsContent novo:
```tsx
<TabsContent value="pages" className="mt-4">
  <ScrollArea className="max-h-96">
    <div className="space-y-1">
      {deletedPages.length === 0 ? (
        <p className="text-sm text-muted-foreground text-center py-6">
          Nenhuma pagina na lixeira.
        </p>
      ) : (
        deletedPages.map((page) => (
          <div
            key={page.id}
            className="flex items-center gap-2 px-2 py-2 rounded-md border"
          >
            <FileText className="w-4 h-4 text-muted-foreground flex-shrink-0" />
            <span className="text-sm flex-1 truncate">{page.title}</span>
            <Button
              size="sm"
              variant="ghost"
              onClick={() => {
                restorePage.mutate(page.id, {
                  onSuccess: () => toast.success('Pagina restaurada'),
                  onError: () => toast.error('Erro ao restaurar'),
                });
              }}
            >
              Restaurar
            </Button>
          </div>
        ))
      )}
    </div>
  </ScrollArea>
</TabsContent>
```

Imports a adicionar:
```typescript
import { useDeletedPages } from '@/hooks/useTrash';
import { useRestorePage } from '@/hooks/useCrudMutations';
import { FileText } from 'lucide-react';
```

E hook calls no componente:
```typescript
const { data: deletedPages = [] } = useDeletedPages();
const restorePage = useRestorePage();
```
</action>
<verify>
  <automated>cd /home/projects/hub-tarefas-lfpro && grep -E "useDeletedPages|restorePage" src/components/workspace/TrashDrawer.tsx | wc -l</automated>
</verify>
<done>Aba Paginas existe na lixeira. Restore funciona.</done>
</task>

<task id="9" type="checkpoint:human-verify">
<files>(visual)</files>
<action>
Verificacao funcional ponta-a-ponta:

1. `npm run dev`
2. Criar uma nova pagina via "+ Pagina" -> deve navegar para `/page/:id` e mostrar editor vazio
3. Digitar titulo "Meu Doc Teste" -> deve aparecer "Editando..." e depois "Salvo"
4. Digitar texto + criar heading via `/` -> deve aparecer "Salvando..." -> "Salvo"
5. Recarregar pagina (F5) -> conteudo persistido
6. Abrir menu (3 dots) -> Excluir pagina -> confirmar -> deve voltar para `/`
7. Abrir lixeira -> aba Paginas -> ver pagina deletada -> Restaurar
8. Confirmar que ela reaparece no sidebar

Critico: nenhum erro `RLS` no console. Verificar que upsert em `pages.content` funciona (banco recebe blocks). Verificar que apos um debounce o `last updated_at` no DB muda.
</action>
<verify>
  <automated>cd /home/projects/hub-tarefas-lfpro && npm run build 2>&1 | tail -3</automated>
</verify>
<done>Usuario confirma fluxo CRUD funcional. Auto-save com indicador. Lixeira com pages funciona.</done>
</task>

## Criterios de Sucesso

- [ ] Rota `/page/:pageId` registrada (lazy + protegida)
- [ ] Page.tsx renderiza header + editor + auto-save
- [ ] Auto-save com debounce 1500ms persiste em `pages.content`
- [ ] Indicador "Salvando.../Salvo" funciona
- [ ] Titulo editavel com debounce 500ms
- [ ] Excluir/Restaurar funciona via lixeira
- [ ] Recarregar pagina mantem conteudo
- [ ] Sem em-dash em comentarios/copy
