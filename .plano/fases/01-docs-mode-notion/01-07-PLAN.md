---
phase: 01-docs-mode-notion
plan: 01-07
type: feature
autonomous: true
wave: 4
depends_on: [01-04]
requirements: [REQ-07]
files_modified:
  - src/hooks/usePageVersions.ts
  - src/components/page/usePageAutoSave.ts
  - src/components/page/PageVersionsPanel.tsx
  - src/pages/Page.tsx
must_haves:
  truths:
    - "Hook `usePageVersions(pageId)` lista versoes ordenadas desc"
    - "Hook `useCreatePageVersion` insere snapshot (content + title) em `page_versions`"
    - "Hook `useRestorePageVersion` aplica conteudo de uma versao na page atual (sem deletar versoes)"
    - "Auto-save cria snapshot a cada N saves (N=5) OU a cada 5 minutos (o que vier primeiro)"
    - "Snapshot tambem e criado antes de um restore (auto-versionamento)"
    - "PageVersionsPanel lista versoes (autor, data relativa), botao Restaurar por versao"
    - "PageHeader menu Historico abre o panel"
    - "Restaurar substitui conteudo no editor (chama editor.replaceBlocks) sem dataloss"
  artifacts:
    - path: "src/hooks/usePageVersions.ts"
      provides: "Query + mutations para page_versions"
    - path: "src/components/page/PageVersionsPanel.tsx"
      provides: "Drawer/Dialog com lista de versoes e restore"
    - path: "src/components/page/usePageAutoSave.ts"
      provides: "Versao atualizada com contador de saves e gatilho de snapshot"
  key_links:
    - from: "usePageAutoSave"
      to: "useCreatePageVersion"
      via: "apos cada N saves bem-sucedidos, chama mutate({ pageId, content, title })"
    - from: "PageVersionsPanel (Restaurar)"
      to: "PageEditor (via callback)"
      via: "Page.tsx mantem ref do editor e chama editor.replaceBlocks(blocks)"
---

# Fase 01 Plano 01-07: Versoes / historico de pagina

**Objetivo:** Persistir historico de mudancas para permitir desfazer 'estragos' grandes ou retornar a uma versao anterior. Snapshot periodico em vez de a cada save (evita explosao de storage). Restore preserva versao atual (cria snapshot antes).

## Achados de Pesquisa (Inline)

- **Decisao tomada (criterio do Claude per CONTEXT.md):** Snapshot a cada 5 saves OU a cada 5 minutos, o que vier primeiro. Argumento:
  - Usuario tipico edita em rajadas (~10 a 30 saves por sessao). 5 saves = ~5 snapshots/sessao = razoavel.
  - 5 minutos = limite superior para sessoes longas sem muitas edicoes (so trocas pontuais).
  - Storage: cada page_version e um jsonb. Para uma page de 500 blocos (~50KB), 5 snapshots = 250KB. Sustentavel.
- **Restore preserva atual:** antes de aplicar `content` antigo, cria um snapshot do estado atual. Garante que nada e perdido.
- **API do BlockNote para restore:** `editor.replaceBlocks(editor.document, newBlocks)` substitui tudo.

## Contexto

@src/components/page/usePageAutoSave.ts — hook a estender (plano 01-04)
@src/components/page/PageHeader.tsx — menu com onOpenHistory
@src/pages/Page.tsx — onde abrir o panel

## Tarefas

<task id="1" type="auto">
<files>src/hooks/usePageVersions.ts</files>
<action>
Hook com query + 2 mutations.

```typescript
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { supabase } from '@/integrations/supabase/client';
import type { PageVersion } from '@/types/page';

export const usePageVersions = (pageId: string | null) =>
  useQuery({
    queryKey: ['page_versions', pageId],
    enabled: !!pageId,
    staleTime: 30 * 1000,
    queryFn: async () => {
      const { data, error } = await supabase
        .from('page_versions')
        .select('id, page_id, content, title, created_by, created_at')
        .eq('page_id', pageId!)
        .order('created_at', { ascending: false })
        .limit(50);
      if (error) throw error;
      return (data ?? []) as PageVersion[];
    },
  });

export const useCreatePageVersion = () => {
  const qc = useQueryClient();
  return useMutation({
    retry: 1,
    mutationFn: async ({
      pageId,
      content,
      title,
    }: {
      pageId: string;
      content: unknown[];
      title?: string;
    }) => {
      const { data: user } = await supabase.auth.getUser();
      const { error } = await supabase
        .from('page_versions')
        .insert({
          page_id: pageId,
          content,
          title: title ?? null,
          created_by: user.user?.id ?? null,
        });
      if (error) throw error;
    },
    onSuccess: (_d, vars) => {
      qc.invalidateQueries({ queryKey: ['page_versions', vars.pageId] });
    },
  });
};

/**
 * Restaura conteudo de uma versao para a page atual.
 * Antes de aplicar, cria um snapshot do estado atual (auto-versionamento).
 */
export const useRestorePageVersion = () => {
  const qc = useQueryClient();
  const createSnapshot = useCreatePageVersion();
  return useMutation({
    retry: 1,
    mutationFn: async ({
      pageId,
      versionContent,
      versionTitle,
      currentContent,
      currentTitle,
    }: {
      pageId: string;
      versionContent: unknown[];
      versionTitle: string | null;
      currentContent: unknown[];
      currentTitle: string;
    }) => {
      // 1. Snapshot do estado atual
      await createSnapshot.mutateAsync({
        pageId,
        content: currentContent,
        title: currentTitle,
      });

      // 2. Aplicar conteudo da versao escolhida
      const { error } = await supabase
        .from('pages')
        .update({
          content: versionContent,
          title: versionTitle ?? currentTitle,
        })
        .eq('id', pageId);
      if (error) throw error;

      return { content: versionContent, title: versionTitle ?? currentTitle };
    },
    onSuccess: (_d, vars) => {
      qc.invalidateQueries({ queryKey: ['page', vars.pageId] });
      qc.invalidateQueries({ queryKey: ['page_versions', vars.pageId] });
      qc.invalidateQueries({ queryKey: ['pages'] });
    },
  });
};
```
</action>
<verify>
  <automated>cd /home/projects/hub-tarefas-lfpro && grep -E "usePageVersions|useCreatePageVersion|useRestorePageVersion" src/hooks/usePageVersions.ts | wc -l</automated>
</verify>
<done>3 hooks exportados.</done>
</task>

<task id="2" type="auto">
<files>src/components/page/usePageAutoSave.ts</files>
<action>
Estender o hook criado em 01-04 para gerenciar contador de saves e timestamp do ultimo snapshot.

Mudancas:

1. Importar:
```typescript
import { useCreatePageVersion } from '@/hooks/usePageVersions';
```

2. Adicionar ao state interno do hook:
```typescript
const saveCountRef = useRef(0);
const lastSnapshotRef = useRef<number>(Date.now());
const createSnapshot = useCreatePageVersion();

const SNAPSHOT_EVERY_N_SAVES = 5;
const SNAPSHOT_MAX_INTERVAL_MS = 5 * 60 * 1000;
```

3. Aceitar param adicional opcional `title` no hook (para passar ao snapshot):
```typescript
interface UsePageAutoSaveOptions {
  pageId: string;
  debounceMs?: number;
  getCurrentTitle?: () => string;
}
```

4. No `flush()`, apos `mutation.mutateAsync` bem-sucedida:
```typescript
saveCountRef.current += 1;
const now = Date.now();
const elapsed = now - lastSnapshotRef.current;
if (
  saveCountRef.current >= SNAPSHOT_EVERY_N_SAVES ||
  elapsed >= SNAPSHOT_MAX_INTERVAL_MS
) {
  saveCountRef.current = 0;
  lastSnapshotRef.current = now;
  // fire and forget; nao bloquear UX
  void createSnapshot.mutateAsync({
    pageId,
    content,
    title: getCurrentTitle?.(),
  }).catch(() => {
    // silencioso - snapshot perdido nao quebra a edicao
  });
}
```

5. Validar que `getCurrentTitle` e capturado em ref para nao virar dependencia que muda a cada render.

NAO usar em-dash em comentarios. Manter API publica (`schedule`, `status`, `flush`).
</action>
<verify>
  <automated>cd /home/projects/hub-tarefas-lfpro && grep -E "SNAPSHOT_EVERY_N_SAVES|useCreatePageVersion" src/components/page/usePageAutoSave.ts | wc -l</automated>
</verify>
<done>Hook cria snapshot a cada 5 saves OU 5 minutos. Falha de snapshot nao quebra edicao.</done>
</task>

<task id="3" type="auto">
<files>src/components/page/PageVersionsPanel.tsx</files>
<action>
Drawer/Dialog com lista de versoes.

```tsx
import React from 'react';
import { Dialog, DialogContent, DialogHeader, DialogTitle } from '@/components/ui/dialog';
import { Button } from '@/components/ui/button';
import { ScrollArea } from '@/components/ui/scroll-area';
import { History, RotateCcw } from 'lucide-react';
import { useProfiles } from '@/hooks/useSupabaseData';
import { usePageVersions, useRestorePageVersion } from '@/hooks/usePageVersions';
import { format } from 'date-fns';
import { ptBR } from 'date-fns/locale';
import { toast } from 'sonner';
import type { Page } from '@/types/page';

interface Props {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  page: Page;
  onAfterRestore: (newContent: unknown[]) => void;
}

const PageVersionsPanel: React.FC<Props> = ({ open, onOpenChange, page, onAfterRestore }) => {
  const { data: versions = [], isLoading } = usePageVersions(page.id);
  const { data: profiles = [] } = useProfiles();
  const restore = useRestorePageVersion();

  const getAuthor = (userId: string | null) => {
    if (!userId) return 'Sistema';
    const p = profiles.find((pr: any) => pr.id === userId);
    return p?.name ?? 'Usuario';
  };

  const handleRestore = (version: typeof versions[number]) => {
    if (!confirm('Restaurar esta versao? O estado atual sera salvo no historico antes.')) return;
    restore.mutate(
      {
        pageId: page.id,
        versionContent: version.content,
        versionTitle: version.title,
        currentContent: page.content,
        currentTitle: page.title,
      },
      {
        onSuccess: (res) => {
          toast.success('Versao restaurada');
          onAfterRestore(res.content);
          onOpenChange(false);
        },
        onError: () => toast.error('Erro ao restaurar versao'),
      }
    );
  };

  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent className="sm:max-w-md">
        <DialogHeader>
          <DialogTitle className="flex items-center gap-2">
            <History className="w-4 h-4" />
            Historico de versoes
          </DialogTitle>
        </DialogHeader>
        <ScrollArea className="max-h-[60vh]">
          {isLoading && (
            <div className="flex justify-center py-6">
              <div className="w-5 h-5 border-2 border-primary border-t-transparent rounded-full animate-spin" />
            </div>
          )}
          {!isLoading && versions.length === 0 && (
            <p className="text-sm text-muted-foreground text-center py-6">
              Sem versoes salvas ainda. Snapshots sao criados automaticamente a cada poucos minutos de edicao.
            </p>
          )}
          <div className="space-y-2">
            {versions.map((v) => (
              <div
                key={v.id}
                className="flex items-center gap-2 px-3 py-2 rounded-md border"
              >
                <div className="min-w-0 flex-1">
                  <p className="text-sm font-medium truncate">
                    {v.title ?? page.title}
                  </p>
                  <p className="text-xs text-muted-foreground">
                    {getAuthor(v.created_by)} ~{' '}
                    {format(new Date(v.created_at), "dd 'de' MMM 'as' HH:mm", { locale: ptBR })}
                  </p>
                </div>
                <Button
                  variant="ghost"
                  size="sm"
                  className="text-xs"
                  onClick={() => handleRestore(v)}
                  disabled={restore.isPending}
                >
                  <RotateCcw className="w-3.5 h-3.5 mr-1" />
                  Restaurar
                </Button>
              </div>
            ))}
          </div>
        </ScrollArea>
      </DialogContent>
    </Dialog>
  );
};

export default PageVersionsPanel;
```

Notas:
- Padrao date-fns + ptBR locale (convencao do projeto).
- `~` em vez de em-dash.
- `confirm()` nativo suficiente no MVP.
- `onAfterRestore` callback permite a Page.tsx atualizar o editor (replaceBlocks).
</action>
<verify>
  <automated>cd /home/projects/hub-tarefas-lfpro && npx tsc --noEmit 2>&1 | grep "PageVersionsPanel" | head -3 || echo "OK"</automated>
</verify>
<done>Panel compila. Lista versoes com autor + data. Restaurar dispara mutation + callback.</done>
</task>

<task id="4" type="auto">
<files>src/pages/Page.tsx</files>
<action>
Conectar panel ao header + obter ref do editor para replaceBlocks.

Mudancas pontuais:

1. State adicional:
```typescript
const [historyOpen, setHistoryOpen] = useState(false);
```

2. Import:
```typescript
import PageVersionsPanel from '@/components/page/PageVersionsPanel';
```

3. Para conseguir replaceBlocks externamente, expor o editor via callback. Refatorar PageEditor para aceitar `onReady?: (editor: BlockNoteEditor) => void`. Ou MAIS SIMPLES: usar uma ref pattern com forwardRef. ABORDAGEM RECOMENDADA:

   - Em PageEditor.tsx: aceitar `editorRef?: React.MutableRefObject<any>` e setar ref.current = editor.
   - Em Page.tsx:
     ```typescript
     const editorRef = useRef<any>(null);
     // <PageEditor editorRef={editorRef} ... />
     ```
   - Em handle de restore:
     ```typescript
     const handleAfterRestore = (newContent: unknown[]) => {
       if (editorRef.current) {
         editorRef.current.replaceBlocks(editorRef.current.document, newContent);
       }
     };
     ```

4. Passar callback ao PageHeader:
```tsx
<PageHeader
  ...
  onOpenHistory={() => setHistoryOpen(true)}
/>
```

5. Renderizar:
```tsx
<PageVersionsPanel
  open={historyOpen}
  onOpenChange={setHistoryOpen}
  page={page as any}
  onAfterRestore={handleAfterRestore}
/>
```

6. Passar getCurrentTitle ao usePageAutoSave:
```typescript
const autoSave = usePageAutoSave({
  pageId: pageId ?? '',
  getCurrentTitle: () => page?.title ?? '',
});
```

Em PageEditor.tsx, ajustar:
```tsx
interface PageEditorProps {
  // existente...
  editorRef?: React.MutableRefObject<any>;
}

// dentro do componente, apos useCreateBlockNote:
useEffect(() => {
  if (editorRef) {
    editorRef.current = editor;
  }
}, [editor, editorRef]);
```
</action>
<verify>
  <automated>cd /home/projects/hub-tarefas-lfpro && grep -E "PageVersionsPanel|historyOpen|editorRef" src/pages/Page.tsx src/components/page/PageEditor.tsx | wc -l</automated>
</verify>
<done>Page abre panel. Restaurar substitui conteudo no editor sem reload manual.</done>
</task>

<task id="5" type="checkpoint:human-verify">
<files>(visual)</files>
<action>
Teste end-to-end:

1. Abrir uma pagina nova
2. Editar 5 vezes em sequencia (texto, heading, lista, callout, divider) com pausas curtas
3. Abrir menu -> Historico -> deve ver pelo menos 1 entrada (snapshot apos 5 saves)
4. Continuar editando por 5+ minutos -> mais 1 snapshot
5. Restaurar uma versao antiga -> confirmar dialog -> editor mostra conteudo antigo, e abrir Historico de novo deve mostrar o snapshot do estado anterior (criado automaticamente antes do restore)
6. SQL check: `select count(*) from page_versions where page_id = '<id>'` deve crescer

Edge cases:
- Sem permissao de admin: usuario viewer NAO deve conseguir restaurar (RLS permite INSERT em page_versions apenas para members; restore tambem updates pages.content que exige can_access_page que ja inclui viewer permission. ACEITAVEL.) Documentar este comportamento como TODO de fase futura ("Restricao de restore para editors+") se for problema. No MVP, considerar aceito.
</action>
<verify>
  <automated>cd /home/projects/hub-tarefas-lfpro && npm run build 2>&1 | tail -3</automated>
</verify>
<done>Snapshots criados periodicamente. Restore aplica conteudo e cria snapshot do estado anterior.</done>
</task>

## Criterios de Sucesso

- [ ] usePageVersions, useCreatePageVersion, useRestorePageVersion exportados
- [ ] usePageAutoSave cria snapshot a cada 5 saves OU 5 minutos
- [ ] PageVersionsPanel lista versoes ordenadas DESC com autor + data ptBR
- [ ] Restaurar dispara replaceBlocks no editor + cria snapshot do estado atual
- [ ] PageHeader menu abre o panel
- [ ] Build passa
