---
phase: 01-docs-mode-notion
plan: 01-03
type: feature
autonomous: true
wave: 2
depends_on: [01-01]
requirements: [REQ-03]
files_modified:
  - src/components/modals/CreateBoardModal.tsx
  - src/components/AppSidebar.tsx
  - src/hooks/useSupabaseData.ts
  - src/hooks/useCrudMutations.ts
must_haves:
  truths:
    - "CreateBoardModal tem toggle 'Tarefas' / 'Pagina' no topo, default 'Tarefas'"
    - "Quando 'Pagina' selecionada o modal pula etapa de template e mostra apenas campo Nome + (opcional) descricao"
    - "Submit cria uma row em `pages` quando tipo='Pagina', e em `boards` quando tipo='Tarefas'"
    - "Apos criacao, navega para `/page/:pageId` (Pagina) ou `/board/:boardId` (Tarefas)"
    - "Sidebar lista boards + pages mesclados (`WorkspaceEntry[]`) ordenados por position, cada um com icone proprio"
    - "Item de page no sidebar tem icone FileText (Lucide) e clica navega para `/page/:pageId`"
    - "Hook `usePages(workspaceId)` retorna `Page[]` filtrado por state='active' com ordenacao position"
    - "Hook `useCreatePage()` insere page, retorna { id }, invalida cache `['pages']` e `['workspace-entries']`"
  artifacts:
    - path: "src/components/modals/CreateBoardModal.tsx"
      provides: "Modal expandido com escolha de tipo Tarefas/Pagina"
    - path: "src/hooks/useSupabaseData.ts"
      provides: "usePages(workspaceId), useAllPages(), useWorkspaceEntries(workspaceId)"
    - path: "src/hooks/useCrudMutations.ts"
      provides: "useCreatePage()"
    - path: "src/components/AppSidebar.tsx"
      provides: "Sidebar mesclado boards + pages"
  key_links:
    - from: "CreateBoardModal (tipo=Pagina)"
      to: "useCreatePage().mutateAsync"
      via: "insert em pages, depois navigate(`/page/${page.id}`)"
    - from: "AppSidebar (WorkspaceItem)"
      to: "useWorkspaceEntries(workspaceId)"
      via: "merge ordenado de boards + pages para renderizar lista"
---

# Fase 01 Plano 01-03: Tipo "Pagina" no modal de criacao + sidebar mista

**Objetivo:** Permitir que o usuario crie pages no mesmo entry point dos boards (`CreateBoardModal`), e veja pages listadas no sidebar do workspace ao lado dos boards. Espinha dorsal da percepcao do usuario de que existe um novo formato.

## Achados de Pesquisa (Inline)

Nenhuma pesquisa externa necessaria. Padroes existentes do projeto:
- `useCreateBoard` em `src/hooks/useCrudMutations.ts:52-83` — padrao a replicar exatamente para `useCreatePage`.
- `useBoards`/`useAllBoards` em `src/hooks/useSupabaseData.ts:46-67` — padrao a replicar para `usePages`/`useAllPages`.
- `CreateBoardModal.tsx` tem state-machine `step: 'template' | 'details'`. Vamos adicionar etapa `'type-select'` antes.
- `AppSidebar.tsx` renderiza boards via `WorkspaceItem`. A logica de filtro por workspace fica dentro do componente; vamos passar `entries: WorkspaceEntry[]` em vez de `boards: Board[]`.

## Contexto

@src/components/modals/CreateBoardModal.tsx — modal a estender
@src/hooks/useCrudMutations.ts — padrao de `useCreateBoard` (linha 52)
@src/hooks/useSupabaseData.ts — padrao de `useBoards`/`useAllBoards` (linha 46)
@src/components/AppSidebar.tsx — sidebar a expandir
@src/types/page.ts — tipo `WorkspaceEntry` discriminado (criado em 01-01)

## Tarefas

<task id="1" type="auto">
<files>src/hooks/useSupabaseData.ts</files>
<action>
Adicionar hooks de leitura de pages, espelhando `useBoards`/`useAllBoards`. Inserir logo apos a secao `// ---- Boards ----` (linha ~67).

Codigo literal a INSERIR:

```typescript
// ---- Pages ----
export const usePages = (workspaceId?: string) =>
  useQuery({
    queryKey: ['pages', workspaceId],
    enabled: !!workspaceId,
    staleTime: 2 * 60 * 1000,
    queryFn: async () => {
      const { data, error } = await supabase
        .from('pages')
        .select('id, workspace_id, folder_id, title, state, icon, cover_url, position, created_by, created_at, updated_at')
        .eq('workspace_id', workspaceId!)
        .eq('state', 'active')
        .order('position')
        .order('created_at');
      if (error) throw error;
      return data ?? [];
    },
  });

export const useAllPages = () =>
  useQuery({
    queryKey: ['all-pages'],
    staleTime: 2 * 60 * 1000,
    queryFn: async () => {
      const { data, error } = await supabase
        .from('pages')
        .select('id, workspace_id, folder_id, title, state, icon, cover_url, position, created_by, created_at, updated_at')
        .eq('state', 'active')
        .order('position')
        .order('created_at');
      if (error) throw error;
      return data ?? [];
    },
  });

/**
 * Retorna entries mesclados (boards + pages) ordenados por position.
 * Consumido pelo AppSidebar para listar tudo do workspace junto.
 */
export const useWorkspaceEntries = (workspaceId?: string) => {
  const boards = useBoards(workspaceId);
  const pages = usePages(workspaceId);
  const isLoading = boards.isLoading || pages.isLoading;
  const data: import('@/types/page').WorkspaceEntry[] = [
    ...(boards.data ?? []).map((b) => ({
      kind: 'board' as const,
      id: b.id,
      name: b.name,
      icon: b.icon,
      color: b.color,
      folder_id: b.folder_id,
      position: b.position ?? 0,
      workspace_id: b.workspace_id!,
    })),
    ...(pages.data ?? []).map((p) => ({
      kind: 'page' as const,
      id: p.id,
      title: p.title,
      icon: p.icon,
      folder_id: p.folder_id,
      position: p.position ?? 0,
      workspace_id: p.workspace_id!,
    })),
  ].sort((a, b) => a.position - b.position);
  return { data, isLoading };
};
```

NAO usar `select('*')`. NAO esquecer `if (error) throw error`. Retorno fallback `data ?? []` (padrao do projeto).
</action>
<verify>
  <automated>cd /home/projects/hub-tarefas-lfpro && grep -E "usePages|useAllPages|useWorkspaceEntries" src/hooks/useSupabaseData.ts | wc -l</automated>
</verify>
<done>3 hooks novos exportados. `npm run build` passa.</done>
</task>

<task id="2" type="auto">
<files>src/hooks/useCrudMutations.ts</files>
<action>
Adicionar `useCreatePage` espelhando `useCreateBoard` (linha 52). Inserir LOGO DEPOIS de `useCreateBoard`.

Codigo literal:

```typescript
export const useCreatePage = () => {
  const qc = useQueryClient();
  return useMutation({
    retry: 1,
    mutationFn: async ({
      workspaceId,
      title,
      icon,
    }: {
      workspaceId: string;
      title: string;
      icon?: string;
    }) => {
      const { data: user } = await supabase.auth.getUser();
      if (!user.user) throw new Error('Not authenticated');

      // Posicionar no final usando mesma logica de useCreateBoard
      const { data: lastPage } = await supabase
        .from('pages')
        .select('position')
        .eq('workspace_id', workspaceId)
        .eq('state', 'active')
        .order('position', { ascending: false })
        .limit(1);
      const maxPosition = lastPage?.[0]?.position ?? 0;

      const { data, error } = await supabase
        .from('pages')
        .insert({
          workspace_id: workspaceId,
          title,
          icon: icon ?? null,
          content: [],
          position: maxPosition + 1000,
          created_by: user.user.id,
        })
        .select()
        .single();
      if (error) throw error;
      return data;
    },
    onSuccess: () => {
      qc.invalidateQueries({ queryKey: ['pages'] });
      qc.invalidateQueries({ queryKey: ['all-pages'] });
    },
  });
};

export const useDeletePage = () => {
  const qc = useQueryClient();
  return useMutation({
    retry: 1,
    mutationFn: async (id: string) => {
      const { error } = await supabase
        .from('pages')
        .update({ state: 'deleted' })
        .eq('id', id);
      if (error) throw error;
    },
    onSuccess: () => {
      qc.invalidateQueries({ queryKey: ['pages'] });
      qc.invalidateQueries({ queryKey: ['all-pages'] });
    },
  });
};

export const useRenamePage = () => {
  const qc = useQueryClient();
  return useMutation({
    retry: 1,
    mutationFn: async ({ pageId, title }: { pageId: string; title: string }) => {
      const { error } = await supabase
        .from('pages')
        .update({ title })
        .eq('id', pageId);
      if (error) throw error;
    },
    onSuccess: () => {
      qc.invalidateQueries({ queryKey: ['pages'] });
      qc.invalidateQueries({ queryKey: ['all-pages'] });
      qc.invalidateQueries({ queryKey: ['page'] });
    },
  });
};
```

Padroes obrigatorios (do CONVENTIONS.md):
- `retry: 1`
- Auth check inline
- Soft delete via `state: 'deleted'`
- Invalida queries relacionadas em onSuccess
</action>
<verify>
  <automated>cd /home/projects/hub-tarefas-lfpro && grep -E "useCreatePage|useDeletePage|useRenamePage" src/hooks/useCrudMutations.ts | wc -l</automated>
</verify>
<done>3 mutations exportadas. Tipos batem com schema da migration.</done>
</task>

<task id="3" type="auto">
<files>src/components/modals/CreateBoardModal.tsx</files>
<action>
Adicionar etapa inicial `type-select` ao state machine do modal. Quando tipo='page', pular template e ir direto a `details` com campo Nome + Icone (emoji opcional, reusar EmojiColorPicker se simples — caso contrario, deixar so input de nome). NAO mexer no fluxo de board (zero regressao).

Mudancas pontuais:

1. Trocar tipo do `step`:
```typescript
const [step, setStep] = useState<'type-select' | 'template' | 'details'>('type-select');
const [creationType, setCreationType] = useState<'board' | 'page'>('board');
```

2. Importar mutations e hook:
```typescript
import { useCreatePage } from '@/hooks/useCrudMutations';
```

3. Adicionar:
```typescript
const createPage = useCreatePage();
```

4. No `resetState`:
```typescript
const resetState = () => {
  setStep('type-select');
  setCreationType('board');
  setSelectedTemplate(defaultTemplates.find(t => t.id === 'blank')!);
  setName('');
  setDescription('');
  setIsCreating(false);
};
```

5. Adicionar handler de selecao de tipo:
```typescript
const handleSelectType = (type: 'board' | 'page') => {
  setCreationType(type);
  if (type === 'board') {
    setStep('template');
  } else {
    setStep('details');
  }
};
```

6. Adicionar branch no `handleSubmit` para `creationType === 'page'`:
```typescript
const handleSubmit = async (e: React.FormEvent) => {
  e.preventDefault();
  if (!name.trim()) return;
  setIsCreating(true);
  try {
    if (creationType === 'page') {
      const page = await createPage.mutateAsync({
        workspaceId,
        title: name.trim(),
      });
      toast.success('Pagina criada com sucesso!');
      resetState();
      onOpenChange(false);
      navigate(`/page/${page.id}`);
      return;
    }

    // Caminho Board (original, NAO alterar logica abaixo)
    const board = await createBoard.mutateAsync({
      workspaceId,
      name: name.trim(),
      description: description.trim() || undefined,
    });
    if (selectedTemplate.id !== 'blank') {
      const { data: authData } = await supabase.auth.getUser();
      const userId = authData.user?.id ?? '';
      await applyTemplate({ boardId: board.id, template: selectedTemplate, userId });
    }
    toast.success('Board criado com sucesso!');
    resetState();
    onOpenChange(false);
    navigate(`/board/${board.id}`);
  } catch {
    toast.error(creationType === 'page' ? 'Erro ao criar pagina' : 'Erro ao criar board');
    setIsCreating(false);
  }
};
```

7. Renderizar etapa `type-select` ANTES do branch `step === 'template'` ja existente. Layout 2 cards lado-a-lado:

```tsx
{step === 'type-select' ? (
  <>
    <div className="space-y-3 py-2">
      <p className="text-sm text-muted-foreground">
        Escolha o que voce quer criar
      </p>
      <div className="grid grid-cols-2 gap-3">
        <button
          type="button"
          onClick={() => handleSelectType('board')}
          className="p-4 rounded-lg border-2 border-border hover:border-primary/50 hover:bg-accent/50 transition-all text-left"
        >
          <div className="text-2xl mb-2">📊</div>
          <div className="font-medium text-sm">Tarefas (Board)</div>
          <div className="text-xs text-muted-foreground mt-1">
            Tabela, Kanban, Calendario. Para gestao de itens e fluxos.
          </div>
        </button>
        <button
          type="button"
          onClick={() => handleSelectType('page')}
          className="p-4 rounded-lg border-2 border-border hover:border-primary/50 hover:bg-accent/50 transition-all text-left"
        >
          <div className="text-2xl mb-2">📄</div>
          <div className="font-medium text-sm">Pagina (Doc)</div>
          <div className="text-xs text-muted-foreground mt-1">
            Documento estilo Notion. Para anotacoes, RFCs, runbooks.
          </div>
        </button>
      </div>
    </div>
    <DialogFooter className="pt-4 border-t">
      <Button type="button" variant="outline" onClick={() => handleOpenChange(false)}>
        Cancelar
      </Button>
    </DialogFooter>
  </>
) : step === 'template' ? (
  // codigo original do template selector...
) : (
  // codigo original do details form...
)}
```

8. Para `step === 'details'` quando `creationType === 'page'`, NAO mostrar o card de template selecionado. Adicionar guard:
```tsx
{step === 'details' && creationType === 'board' && (
  <div className="flex items-center gap-3 p-3 rounded-lg bg-accent/50 border">
    <span className="text-2xl">{selectedTemplate.icon}</span>
    <div>
      <div className="font-medium text-sm">{selectedTemplate.name}</div>
      <div className="font-density-cell text-muted-foreground">{selectedTemplate.description}</div>
    </div>
  </div>
)}
```

9. No campo Nome, atualizar label dinamicamente:
```tsx
<Label htmlFor="board-name" className="monday-text2">
  {creationType === 'page' ? 'Titulo da pagina' : 'Nome'}
</Label>
```

10. No botao submit:
```tsx
<Button type="submit" disabled={!name.trim() || isCreating}>
  {isCreating
    ? 'Criando...'
    : creationType === 'page'
      ? 'Criar Pagina'
      : 'Criar Board'}
</Button>
```

11. Atualizar `DialogTitle`:
```tsx
<DialogTitle className="monday-h4 font-heading">
  {step === 'type-select'
    ? 'Criar novo'
    : step === 'template'
      ? 'Escolha um Template'
      : creationType === 'page'
        ? 'Configurar Pagina'
        : 'Configurar Board'}
</DialogTitle>
```

12. No `handleBack` (botao Voltar do details), considerar `creationType`:
```typescript
const handleBack = () => {
  if (creationType === 'page') {
    setStep('type-select');
  } else {
    setStep('template');
  }
};
```

NAO usar em-dash. Sem `any`.
</action>
<verify>
  <automated>cd /home/projects/hub-tarefas-lfpro && grep -E "creationType|useCreatePage|type-select" src/components/modals/CreateBoardModal.tsx | wc -l</automated>
</verify>
<done>Modal exibe 3 etapas (type-select -> template/details -> details). Fluxo de board nao regride. Fluxo de page navega para /page/:id (rota nao existe ainda — sera plano 01-04).</done>
</task>

<task id="4" type="auto">
<files>src/components/AppSidebar.tsx</files>
<action>
Adaptar o componente `WorkspaceItem` (linha 79+) para listar `WorkspaceEntry[]` mesclado em vez de apenas boards. Trabalho cirurgico, sem reescrever o componente.

Mudancas pontuais:

1. No topo do arquivo, importar:
```typescript
import { useWorkspaceEntries } from '@/hooks/useSupabaseData';
import { FileText, Layout } from 'lucide-react';
import type { WorkspaceEntry } from '@/types/page';
```

2. Dentro de `WorkspaceItem` (componente memoizado, linha ~79), substituir a chamada que pega boards do workspace por:
```typescript
const { data: entries = [], isLoading: entriesLoading } = useWorkspaceEntries(workspace.id);
```
(Conferir a busca atual — provavelmente usa `useBoards(workspace.id)`. Manter essa busca tambem se a logica de DnD em boards depender disso. SIMPLIFICACAO: ADICIONAR `useWorkspaceEntries` como fonte de listagem, sem remover `useBoards` que serve para outras coisas.)

3. Substituir o `.map(board => ...)` da lista do workspace por:
```tsx
{entries.map((entry: WorkspaceEntry) => {
  if (entry.kind === 'board') {
    // renderizar item de board (logica original)
    return <BoardSidebarItem key={`board-${entry.id}`} board={entry} ... />;
  }
  // renderizar item de page
  return (
    <button
      key={`page-${entry.id}`}
      onClick={() => navigate(`/page/${entry.id}`)}
      className={`group flex items-center gap-2 w-full px-2 py-1.5 rounded-md text-left hover:bg-accent/50 transition-colors ${
        location.pathname === `/page/${entry.id}` ? 'bg-accent text-foreground' : 'text-muted-foreground'
      }`}
    >
      <span className="flex-shrink-0">
        {entry.icon ? <span className="text-sm">{entry.icon}</span> : <FileText className="w-3.5 h-3.5" />}
      </span>
      <span className="text-sm truncate flex-1">{entry.title}</span>
    </button>
  );
})}
```

(Se ja existir um subcomponente para board no arquivo, extrair sem mudar a logica e wrapping na branch. SE NAO existir extracao, manter inline mas dentro do if `entry.kind === 'board'`.)

4. Garantir que o counter `itemCounts` so afeta boards (passar `if (entry.kind === 'board')` no calculo).

5. NAO usar em-dash. NAO usar `any` no map.

Cuidado: AppSidebar.tsx tem 696 linhas. Foco SOMENTE na area de listagem do workspace. NAO mexer em:
- DnD de boards (manter)
- Drag handles
- Logica de favoritos (apenas funciona para boards no MVP)
- Folders (`workspace_folders`) — pages podem ir em folders no futuro mas no MVP nao precisam aparecer nelas

Se ficar muito custoso editar inline, criar componente auxiliar `src/components/PageSidebarItem.tsx` com o button acima e importar no AppSidebar.tsx. Esta opcao e PREFERIDA para isolar mudanca.
</action>
<verify>
  <automated>cd /home/projects/hub-tarefas-lfpro && grep -E "useWorkspaceEntries|WorkspaceEntry|kind === 'page'" src/components/AppSidebar.tsx | wc -l</automated>
</verify>
<done>Sidebar renderiza pages com icone FileText, navega para /page/:id ao clicar. Boards continuam funcionando.</done>
</task>

<task id="5" type="checkpoint:human-verify">
<files>(visual)</files>
<action>
Visual check do modal e sidebar.

Passos:
1. `npm run dev`
2. Abrir app, clicar em "+" no workspace
3. Confirmar que o modal mostra duas opcoes: "Tarefas (Board)" e "Pagina (Doc)"
4. Selecionar "Pagina" -> deve mostrar apenas campo de titulo
5. Submeter -> deve navegar para `/page/:id` (provavelmente 404 ate plano 01-04)
6. Voltar para `/` -> confirmar que a page aparece no sidebar com icone de documento

Page criada deve estar persistida no banco (verificar via SQL editor do Supabase: `select id, title from pages where state = 'active' order by created_at desc limit 5;`).
</action>
<verify>
  <automated>cd /home/projects/hub-tarefas-lfpro && npm run build 2>&1 | tail -5 | grep -E "built|error"</automated>
</verify>
<done>Usuario confirma que o fluxo Modal -> Sidebar funciona. Page aparece no sidebar mesmo que a rota /page/:id ainda nao renderize editor.</done>
</task>

## Criterios de Sucesso

- [ ] CreateBoardModal expandido com toggle Tarefas/Pagina
- [ ] usePages, useAllPages, useWorkspaceEntries exportados
- [ ] useCreatePage, useDeletePage, useRenamePage exportados
- [ ] AppSidebar renderiza boards + pages mesclados
- [ ] Criar pagina persiste em `pages` table
- [ ] Page aparece no sidebar com icone proprio
- [ ] Build e dev continuam passando
