---
phase: 01-docs-mode-notion
plan: 01-06
type: feature
autonomous: true
wave: 4
depends_on: [01-04]
requirements: [REQ-06]
files_modified:
  - src/hooks/usePagePermissions.ts
  - src/components/page/PagePermissionsPanel.tsx
  - src/components/page/PageHeader.tsx
  - src/pages/Page.tsx
must_haves:
  truths:
    - "Hook `usePagePermissions(pageId)` lista permissoes da page (espelha `usePermissions` de board)"
    - "Hook `usePageRole(pageId)` retorna role do usuario corrente (admin/editor/member/viewer)"
    - "Hooks `useCanEditPage` e `useCanAdminPage` derivam booleanos a partir do role"
    - "Mutations `useSetPagePermission` e `useRemovePagePermission` inseridas (CRUD em `page_permissions`)"
    - "Componente `PagePermissionsPanel` espelha `BoardPermissionsPanel` (adicionar/remover/atualizar membros)"
    - "PageHeader integra botao Permissoes que abre o panel"
    - "Usuario sem permissao (RLS bloqueia) ve estado 'Pagina nao encontrada' (ja tratado em 01-04)"
    - "Editor entra em modo `editable={false}` quando o usuario tem role viewer/member"
  artifacts:
    - path: "src/hooks/usePagePermissions.ts"
      provides: "Hooks de permissao espelhando usePermissions.ts"
    - path: "src/components/page/PagePermissionsPanel.tsx"
      provides: "UI completa de gestao de membros da page"
  key_links:
    - from: "PageHeader (menu)"
      to: "PagePermissionsPanel"
      via: "onClick abre o panel via state local da Page.tsx"
    - from: "Page.tsx"
      to: "useCanEditPage"
      via: "computa flag e passa como prop editable do PageEditor"
---

# Fase 01 Plano 01-06: Permissoes de pagina

**Objetivo:** Espelhar exatamente o sistema de permissoes de boards para pages. Reusa modelo de roles (admin/editor/member/viewer), reusa visual de panel, mas opera sobre `page_permissions`. Critico para REQ-06.

## Achados de Pesquisa (Inline)

Trabalho 100% baseado em padroes existentes. Sem pesquisa externa.

- `src/hooks/usePermissions.ts` ja tem o template completo (75 linhas). Vamos clonar trocando boards -> pages e queryKey de `board_permissions` -> `page_permissions`.
- `src/components/board/BoardPermissionsPanel.tsx` (233 linhas) tambem clonavel.

## Contexto

@src/hooks/usePermissions.ts — template a espelhar
@src/components/board/BoardPermissionsPanel.tsx — UI a espelhar
@src/components/page/PageHeader.tsx — onde adicionar o trigger
@src/pages/Page.tsx — onde controlar editable via role

## Tarefas

<task id="1" type="auto">
<files>src/hooks/usePagePermissions.ts</files>
<action>
Criar arquivo seguindo padrao identico a `usePermissions.ts`.

```typescript
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { supabase } from '@/integrations/supabase/client';
import { useAuth } from '@/hooks/useAuth';
import type { PagePermission, PageRole } from '@/types/page';

export const usePagePermissions = (pageId: string | null) =>
  useQuery({
    queryKey: ['page_permissions', pageId],
    enabled: !!pageId,
    queryFn: async () => {
      const { data, error } = await supabase
        .from('page_permissions')
        .select('id, page_id, user_id, role, created_at')
        .eq('page_id', pageId!);
      if (error) throw error;
      return (data ?? []) as PagePermission[];
    },
  });

export const usePageRole = (pageId: string | null): PageRole | null => {
  const { user } = useAuth();
  const { data: permissions = [] } = usePagePermissions(pageId);
  if (!user || !pageId) return null;
  const my = permissions.find((p) => p.user_id === user.id);
  return (my?.role as PageRole | undefined) ?? null;
};

/**
 * Usuario pode editar se for membro do workspace (acesso default) E nao for viewer explicito.
 * Sem registro em page_permissions => assumir editor (workspace member tem edicao por default).
 * Com registro role 'viewer' => bloqueia.
 */
export const useCanEditPage = (pageId: string | null): boolean => {
  const role = usePageRole(pageId);
  if (role === null) return true; // sem registro explicito = editor default
  return role === 'admin' || role === 'editor' || role === 'member';
};

export const useCanAdminPage = (pageId: string | null): boolean => {
  const role = usePageRole(pageId);
  return role === 'admin';
};

export const useSetPagePermission = () => {
  const qc = useQueryClient();
  return useMutation({
    retry: 1,
    mutationFn: async ({
      pageId,
      userId,
      role,
    }: {
      pageId: string;
      userId: string;
      role: PageRole;
    }) => {
      const { data: existing } = await supabase
        .from('page_permissions')
        .select('id')
        .eq('page_id', pageId)
        .eq('user_id', userId)
        .maybeSingle();

      if (existing) {
        const { error } = await supabase
          .from('page_permissions')
          .update({ role })
          .eq('id', existing.id);
        if (error) throw error;
      } else {
        const { error } = await supabase
          .from('page_permissions')
          .insert({ page_id: pageId, user_id: userId, role });
        if (error) throw error;
      }
    },
    onSuccess: () => {
      qc.invalidateQueries({ queryKey: ['page_permissions'] });
    },
  });
};

export const useRemovePagePermission = () => {
  const qc = useQueryClient();
  return useMutation({
    retry: 1,
    mutationFn: async ({
      pageId,
      userId,
    }: {
      pageId: string;
      userId: string;
    }) => {
      const { error } = await supabase
        .from('page_permissions')
        .delete()
        .eq('page_id', pageId)
        .eq('user_id', userId);
      if (error) throw error;
    },
    onSuccess: () => {
      qc.invalidateQueries({ queryKey: ['page_permissions'] });
    },
  });
};
```

Padroes obrigatorios: retry: 1, queryKey `['page_permissions', pageId]`, tipo importado de `@/types/page`.
</action>
<verify>
  <automated>cd /home/projects/hub-tarefas-lfpro && grep -E "usePagePermissions|usePageRole|useCanEditPage|useCanAdminPage|useSetPagePermission|useRemovePagePermission" src/hooks/usePagePermissions.ts | wc -l</automated>
</verify>
<done>6 hooks exportados. Tipos corretos. Compila.</done>
</task>

<task id="2" type="auto">
<files>src/components/page/PagePermissionsPanel.tsx</files>
<action>
Clonar `BoardPermissionsPanel.tsx` (linha por linha) trocando ocorrencias:
- `BoardPermissionsPanel` -> `PagePermissionsPanel`
- `boardId` -> `pageId`
- `usePermissions` -> `usePagePermissions`
- `useSetBoardPermission` -> `useSetPagePermission`
- `useRemoveBoardPermission` -> `useRemovePagePermission`
- "Permissoes do Board" -> "Permissoes da Pagina"
- Manter ROLES array literalmente igual

Codigo final esperado (verifique linha a linha contra `BoardPermissionsPanel.tsx`):

```tsx
import React, { useState } from 'react';
import { Dialog, DialogContent, DialogHeader, DialogTitle } from '@/components/ui/dialog';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Avatar, AvatarFallback, AvatarImage } from '@/components/ui/avatar';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';
import { ScrollArea } from '@/components/ui/scroll-area';
import { Trash2, UserPlus, Shield } from 'lucide-react';
import { useProfiles } from '@/hooks/useSupabaseData';
import {
  usePagePermissions,
  useSetPagePermission,
  useRemovePagePermission,
} from '@/hooks/usePagePermissions';
import type { PageRole } from '@/types/page';
import { toast } from 'sonner';

interface Props {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  pageId: string;
}

const ROLES: Array<{ value: PageRole; label: string }> = [
  { value: 'admin', label: 'Admin' },
  { value: 'editor', label: 'Editor' },
  { value: 'member', label: 'Membro' },
  { value: 'viewer', label: 'Visualizador' },
];

const PagePermissionsPanel: React.FC<Props> = ({ open, onOpenChange, pageId }) => {
  const [searchTerm, setSearchTerm] = useState('');
  const [showAddUser, setShowAddUser] = useState(false);

  const { data: permissions = [], isLoading } = usePagePermissions(pageId);
  const { data: profiles = [] } = useProfiles();
  const setPermission = useSetPagePermission();
  const removePermission = useRemovePagePermission();

  const permissionUserIds = permissions.map((p) => p.user_id);

  const availableUsers = profiles.filter(
    (p: any) =>
      !permissionUserIds.includes(p.id) &&
      (p.name?.toLowerCase().includes(searchTerm.toLowerCase()) ||
        p.email?.toLowerCase().includes(searchTerm.toLowerCase()))
  );

  const getProfile = (userId: string) => profiles.find((p: any) => p.id === userId);

  const getInitials = (name: string | null | undefined) => {
    if (!name) return '?';
    return name.split(' ').map((n) => n[0]).join('').slice(0, 2).toUpperCase();
  };

  const handleAddUser = (userId: string) => {
    setPermission.mutate(
      { pageId, userId, role: 'editor' },
      {
        onSuccess: () => {
          toast.success('Membro adicionado');
          setSearchTerm('');
          setShowAddUser(false);
        },
        onError: () => toast.error('Erro ao adicionar membro'),
      }
    );
  };

  const handleUpdateRole = (userId: string, role: string) => {
    setPermission.mutate(
      { pageId, userId, role: role as PageRole },
      {
        onSuccess: () => toast.success('Permissao atualizada'),
        onError: () => toast.error('Erro ao atualizar permissao'),
      }
    );
  };

  const handleRemove = (userId: string) => {
    removePermission.mutate(
      { pageId, userId },
      {
        onSuccess: () => toast.success('Membro removido'),
        onError: () => toast.error('Erro ao remover membro'),
      }
    );
  };

  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent className="sm:max-w-md">
        <DialogHeader>
          <DialogTitle className="flex items-center gap-2">
            <Shield className="w-4 h-4" />
            Permissoes da Pagina
          </DialogTitle>
        </DialogHeader>
        <div className="space-y-4">
          {showAddUser ? (
            <div className="space-y-2">
              <Input
                placeholder="Buscar por nome ou email..."
                value={searchTerm}
                onChange={(e) => setSearchTerm(e.target.value)}
                autoFocus
              />
              <ScrollArea className="max-h-40">
                <div className="space-y-1">
                  {availableUsers.length === 0 && searchTerm && (
                    <p className="text-sm text-muted-foreground py-2 text-center">
                      Nenhum usuario encontrado
                    </p>
                  )}
                  {availableUsers.map((user: any) => (
                    <button
                      key={user.id}
                      onClick={() => handleAddUser(user.id)}
                      className="flex items-center gap-2 w-full px-2 py-1.5 rounded-md hover:bg-muted transition-colors text-left"
                    >
                      <Avatar className="h-6 w-6">
                        <AvatarImage src={user.avatar_url} />
                        <AvatarFallback className="text-xs">
                          {getInitials(user.name)}
                        </AvatarFallback>
                      </Avatar>
                      <div className="min-w-0 flex-1">
                        <p className="text-sm font-medium truncate">{user.name}</p>
                        <p className="text-xs text-muted-foreground truncate">{user.email}</p>
                      </div>
                    </button>
                  ))}
                </div>
              </ScrollArea>
              <Button
                variant="ghost"
                size="sm"
                onClick={() => {
                  setShowAddUser(false);
                  setSearchTerm('');
                }}
              >
                Cancelar
              </Button>
            </div>
          ) : (
            <Button
              variant="outline"
              size="sm"
              className="w-full"
              onClick={() => setShowAddUser(true)}
            >
              <UserPlus className="w-3.5 h-3.5 mr-2" />
              Adicionar membro
            </Button>
          )}
          <ScrollArea className="max-h-64">
            <div className="space-y-2">
              {isLoading && (
                <div className="flex justify-center py-4">
                  <div className="w-5 h-5 border-2 border-primary border-t-transparent rounded-full animate-spin" />
                </div>
              )}
              {permissions.map((perm) => {
                const profile = getProfile(perm.user_id);
                return (
                  <div
                    key={perm.id}
                    className="flex items-center gap-2 px-2 py-1.5 rounded-md border"
                  >
                    <Avatar className="h-7 w-7">
                      <AvatarImage src={profile?.avatar_url} />
                      <AvatarFallback className="text-xs">
                        {getInitials(profile?.name)}
                      </AvatarFallback>
                    </Avatar>
                    <div className="min-w-0 flex-1">
                      <p className="text-sm font-medium truncate">{profile?.name || 'Usuario'}</p>
                      <p className="text-xs text-muted-foreground truncate">{profile?.email}</p>
                    </div>
                    <Select
                      value={perm.role}
                      onValueChange={(val) => handleUpdateRole(perm.user_id, val)}
                    >
                      <SelectTrigger className="w-[130px] h-7 text-xs">
                        <SelectValue />
                      </SelectTrigger>
                      <SelectContent>
                        {ROLES.map((r) => (
                          <SelectItem key={r.value} value={r.value}>
                            {r.label}
                          </SelectItem>
                        ))}
                      </SelectContent>
                    </Select>
                    <Button
                      variant="ghost"
                      size="icon"
                      className="h-7 w-7 text-muted-foreground hover:text-destructive"
                      onClick={() => handleRemove(perm.user_id)}
                    >
                      <Trash2 className="w-3.5 h-3.5" />
                    </Button>
                  </div>
                );
              })}
              {!isLoading && permissions.length === 0 && (
                <p className="text-sm text-muted-foreground text-center py-4">
                  Nenhum membro adicionado a esta pagina
                </p>
              )}
            </div>
          </ScrollArea>
        </div>
      </DialogContent>
    </Dialog>
  );
};

export default PagePermissionsPanel;
```
</action>
<verify>
  <automated>cd /home/projects/hub-tarefas-lfpro && npx tsc --noEmit 2>&1 | grep "PagePermissionsPanel" | head -3 || echo "OK"</automated>
</verify>
<done>Componente compila. Mesmo layout do BoardPermissionsPanel, mas operando sobre page_permissions.</done>
</task>

<task id="3" type="auto">
<files>src/pages/Page.tsx</files>
<action>
Integrar o panel na pagina. Adicionar:

1. State local para abrir/fechar panel:
```typescript
const [permissionsOpen, setPermissionsOpen] = useState(false);
```

2. Importar:
```typescript
import { useState } from 'react';
import PagePermissionsPanel from '@/components/page/PagePermissionsPanel';
import { useCanEditPage } from '@/hooks/usePagePermissions';
```

3. Computar canEdit:
```typescript
const canEdit = useCanEditPage(pageId ?? null);
```

4. Passar callback ao PageHeader:
```tsx
<PageHeader
  pageId={page.id}
  initialTitle={page.title}
  saveStatus={autoSave.status}
  onOpenPermissions={() => setPermissionsOpen(true)}
/>
```

5. Passar `editable={canEdit}` ao PageEditor:
```tsx
<PageEditor
  initialContent={initialContent}
  onChange={(blocks) => autoSave.schedule(blocks)}
  editable={canEdit}
/>
```

6. Renderizar o panel apos o editor (fim do return):
```tsx
<PagePermissionsPanel
  open={permissionsOpen}
  onOpenChange={setPermissionsOpen}
  pageId={page.id}
/>
```
</action>
<verify>
  <automated>cd /home/projects/hub-tarefas-lfpro && grep -E "PagePermissionsPanel|useCanEditPage|setPermissionsOpen" src/pages/Page.tsx | wc -l</automated>
</verify>
<done>Page.tsx abre painel via menu. editable={canEdit} controla read-only.</done>
</task>

<task id="4" type="checkpoint:human-verify">
<files>(visual)</files>
<action>
Cenarios de teste:

1. Como admin global: criar uma pagina, abrir menu -> Permissoes -> adicionar outro usuario como Viewer
2. Logar como Viewer adicionado -> abrir a pagina -> editor deve estar read-only (sem cursor de edicao)
3. Voltar para admin: trocar role do user para Editor -> usuario edita normalmente
4. Remover permissao -> usuario deve poder acessar (e workspace member) com role default editor
5. Como user nao-membro do workspace: tentar abrir URL `/page/:id` -> "Pagina nao encontrada"

Edge: criar uma pagina, logar como outro workspace member -> deve ABRIR e editar por default (sem registro em page_permissions = editor implicito).
</action>
<verify>
  <automated>cd /home/projects/hub-tarefas-lfpro && npm run build 2>&1 | tail -3</automated>
</verify>
<done>Usuario confirma cenarios. RLS bloqueia acesso indevido. Viewer ve read-only.</done>
</task>

## Criterios de Sucesso

- [ ] usePagePermissions, usePageRole, useCanEditPage, useCanAdminPage hooks criados
- [ ] useSetPagePermission, useRemovePagePermission mutations criadas
- [ ] PagePermissionsPanel renderiza membros, permite adicionar/atualizar/remover
- [ ] PageHeader abre o panel via menu
- [ ] editable do editor responde ao role
- [ ] RLS bloqueia acesso para nao-membros
