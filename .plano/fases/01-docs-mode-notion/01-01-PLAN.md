---
phase: 01-docs-mode-notion
plan: 01-01
type: feature
autonomous: true
wave: 1
depends_on: []
requirements: [REQ-01]
files_modified:
  - supabase/migrations/20260522110000_pages_schema.sql
  - src/integrations/supabase/types.ts
  - src/types/page.ts
must_haves:
  truths:
    - "Tabela `pages` existe com colunas (id, workspace_id, folder_id, title, content jsonb, state, icon, cover_url, created_by, created_at, updated_at)"
    - "Tabela `page_versions` existe com (id, page_id, content jsonb, created_by, created_at)"
    - "RPC `can_access_page(_user_id, _page_id)` retorna BOOL e espelha logica de `can_access_board`"
    - "RPC `is_page_admin(_user_id, _page_id)` espelha `is_board_admin` (admin do workspace, criador do workspace, admin global, ou permissao explicita)"
    - "Tabela `page_permissions` espelha `board_permissions` com roles admin/editor/member/viewer"
    - "RLS policies bloqueiam SELECT/INSERT/UPDATE/DELETE para nao-membros do workspace"
    - "Realtime publication inclui as 3 tabelas novas"
    - "Tipos TypeScript `Database` regenerados refletem o novo schema"
  artifacts:
    - path: "supabase/migrations/20260522110000_pages_schema.sql"
      provides: "Schema completo de pages + page_versions + page_permissions + RPCs + RLS"
    - path: "src/integrations/supabase/types.ts"
      provides: "Tipos Database atualizados (gerados via supabase gen types)"
    - path: "src/types/page.ts"
      provides: "Interfaces TypeScript de dominio (Page, PageVersion, PagePermission, PageRole)"
  key_links:
    - from: "RLS de pages"
      to: "RPC can_access_page"
      via: "USING/WITH CHECK clauses chamando o RPC"
    - from: "RLS de page_permissions"
      to: "RPC is_page_admin"
      via: "INSERT/UPDATE/DELETE permitidos apenas para page admins"
---

# Fase 01 Plano 01-01: Schema + RPCs Supabase para Pages

**Objetivo:** Criar a fundacao de banco para o modo "Pagina". Espelhar exatamente a arquitetura de `boards` + `board_permissions` (RPCs `can_access_board` e `is_board_admin`) para `pages` + `page_permissions`, garantindo que toda a logica de permissao multi-nivel (global admin / workspace admin / page admin) ja funcione na camada DB. Tabela `page_versions` cobre o requisito de historico (consumida no plano 01-07).

## Achados de Pesquisa (Inline)

- **Padrao existente do projeto** (verificado em `supabase/migrations/20260216230617_*.sql` e `20260219080000_fix_security_rls_policies.sql` e `20260409100000_fix_board_permissions_insert_policy.sql`):
  - RPCs sempre declaradas como `LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public` com assinatura `(_user_id UUID, _entity_id UUID)`
  - `is_board_admin` checa: permissao explicita em `board_permissions` (role admin) OU `workspaces.created_by` OU `workspace_members` com role admin OU `user_roles` global admin. Vamos replicar identicamente.
  - Policies sempre nomeadas em ingles, com TO authenticated, USING/WITH CHECK via RPC
  - Padrao de soft delete: coluna `state TEXT DEFAULT 'active' CHECK (state IN ('active','archived','deleted'))`
  - Coluna `position FLOAT NOT NULL DEFAULT 0` para ordenacao no sidebar (igual boards)
- **Realtime:** projeto ja usa `postgres_changes` em canal `workspace-sync` (`useRealtimeSync.ts`). Para Postgres-changes funcionar precisamos `ALTER PUBLICATION supabase_realtime ADD TABLE`.
- **`is_workspace_member`** assinatura existente: `(_user_id UUID, _workspace_id UUID)` (NAO inverter).

## Contexto

@supabase/migrations/20260216230617_ce0c9eda-aa90-4930-b4e6-d0693832341f.sql — base do schema, define `can_access_board` (linhas 101-110), `is_workspace_member` (linhas 58-63), `has_role` (linhas 27-32)
@supabase/migrations/20260219080000_fix_security_rls_policies.sql — define `is_board_admin` original
@supabase/migrations/20260409100000_fix_board_permissions_insert_policy.sql — versao FINAL de `is_board_admin` (incluir global admin + workspace admin) que vamos espelhar
@supabase/migrations/20260219100000_board_icon_color.sql — exemplo de migration que adiciona `icon TEXT` e `color TEXT` (boards). Pages tambem precisa `icon` (mas usa `cover_url` em vez de `color`).
@src/integrations/supabase/types.ts — tipos gerados (precisa regenerar apos migration)
@src/types/board.ts — interface `Board` que vamos espelhar como `Page`
@src/hooks/usePermissions.ts — modelo de roles `'admin'|'editor'|'member'|'viewer'`

## Tarefas

<task id="1" type="auto">
<files>supabase/migrations/20260522110000_pages_schema.sql</files>
<action>
Criar arquivo de migration novo com timestamp `20260522110000_pages_schema.sql` em `supabase/migrations/`.

Conteudo COMPLETO (copiar literalmente, ajustando apenas se houver conflito):

```sql
-- =============================================================================
-- MIGRATION: Add pages (Docs Mode) + page_versions + page_permissions
-- Date: 2026-05-22
-- Description: Notion-like pages alongside boards. Mirrors boards architecture
--              for permissions (workspace member SELECT, page admin manage).
--              Storage: BlockNote JSON in content jsonb.
-- =============================================================================

-- ----------------------------------------------------------------------------
-- Table: pages
-- ----------------------------------------------------------------------------
CREATE TABLE public.pages (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  workspace_id UUID NOT NULL REFERENCES public.workspaces(id) ON DELETE CASCADE,
  folder_id UUID REFERENCES public.workspace_folders(id) ON DELETE SET NULL,
  title TEXT NOT NULL DEFAULT 'Pagina sem titulo',
  content JSONB NOT NULL DEFAULT '[]'::jsonb,
  state TEXT NOT NULL DEFAULT 'active' CHECK (state IN ('active','archived','deleted')),
  icon TEXT,
  cover_url TEXT,
  position FLOAT NOT NULL DEFAULT 0,
  created_by UUID REFERENCES auth.users(id),
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX idx_pages_workspace_id ON public.pages(workspace_id);
CREATE INDEX idx_pages_folder_id ON public.pages(folder_id);
CREATE INDEX idx_pages_state ON public.pages(state);
CREATE INDEX idx_pages_position ON public.pages(workspace_id, position);

-- updated_at trigger reusing existing helper
CREATE TRIGGER pages_set_updated_at
  BEFORE UPDATE ON public.pages
  FOR EACH ROW EXECUTE FUNCTION public.update_updated_at();

ALTER TABLE public.pages ENABLE ROW LEVEL SECURITY;

-- ----------------------------------------------------------------------------
-- Table: page_versions (snapshots para historico / restore)
-- ----------------------------------------------------------------------------
CREATE TABLE public.page_versions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  page_id UUID NOT NULL REFERENCES public.pages(id) ON DELETE CASCADE,
  content JSONB NOT NULL,
  title TEXT,
  created_by UUID REFERENCES auth.users(id),
  created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX idx_page_versions_page_id ON public.page_versions(page_id, created_at DESC);

ALTER TABLE public.page_versions ENABLE ROW LEVEL SECURITY;

-- ----------------------------------------------------------------------------
-- Table: page_permissions (espelha board_permissions)
-- ----------------------------------------------------------------------------
CREATE TABLE public.page_permissions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  page_id UUID NOT NULL REFERENCES public.pages(id) ON DELETE CASCADE,
  user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  role TEXT NOT NULL DEFAULT 'editor' CHECK (role IN ('admin','editor','member','viewer')),
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE (page_id, user_id)
);

CREATE INDEX idx_page_permissions_page_id ON public.page_permissions(page_id);
CREATE INDEX idx_page_permissions_user_id ON public.page_permissions(user_id);

ALTER TABLE public.page_permissions ENABLE ROW LEVEL SECURITY;

-- ----------------------------------------------------------------------------
-- RPC: can_access_page (espelha can_access_board)
-- Acesso = membro do workspace OU permissao explicita na page OU global admin
-- ----------------------------------------------------------------------------
CREATE OR REPLACE FUNCTION public.can_access_page(_user_id UUID, _page_id UUID)
RETURNS BOOLEAN
LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public
AS $$
  SELECT EXISTS (
    SELECT 1 FROM public.pages p
    JOIN public.workspace_members wm ON wm.workspace_id = p.workspace_id
    WHERE p.id = _page_id AND wm.user_id = _user_id
  )
  OR EXISTS (
    SELECT 1 FROM public.page_permissions pp
    WHERE pp.page_id = _page_id AND pp.user_id = _user_id
  )
  OR EXISTS (
    SELECT 1 FROM public.user_roles ur
    WHERE ur.user_id = _user_id AND ur.role = 'admin'
  )
$$;

-- ----------------------------------------------------------------------------
-- RPC: is_page_admin (espelha is_board_admin v2)
-- Admin = role 'admin' na page OU workspace creator OU workspace admin OU global admin
-- ----------------------------------------------------------------------------
CREATE OR REPLACE FUNCTION public.is_page_admin(_user_id UUID, _page_id UUID)
RETURNS BOOLEAN
LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public
AS $$
  SELECT EXISTS (
    SELECT 1 FROM public.page_permissions pp
    WHERE pp.page_id = _page_id
      AND pp.user_id = _user_id
      AND pp.role = 'admin'
  )
  OR EXISTS (
    SELECT 1 FROM public.pages p
    JOIN public.workspaces w ON w.id = p.workspace_id
    WHERE p.id = _page_id AND w.created_by = _user_id
  )
  OR EXISTS (
    SELECT 1 FROM public.pages p
    JOIN public.workspace_members wm ON wm.workspace_id = p.workspace_id
    WHERE p.id = _page_id AND wm.user_id = _user_id AND wm.role = 'admin'
  )
  OR EXISTS (
    SELECT 1 FROM public.user_roles ur
    WHERE ur.user_id = _user_id AND ur.role = 'admin'
  )
$$;

-- ----------------------------------------------------------------------------
-- RLS: pages (espelha boards)
-- ----------------------------------------------------------------------------
CREATE POLICY "Members can view pages" ON public.pages
  FOR SELECT TO authenticated
  USING (public.can_access_page(auth.uid(), id));

CREATE POLICY "Members can create pages" ON public.pages
  FOR INSERT TO authenticated
  WITH CHECK (public.is_workspace_member(auth.uid(), workspace_id));

CREATE POLICY "Members can update pages" ON public.pages
  FOR UPDATE TO authenticated
  USING (public.can_access_page(auth.uid(), id));

CREATE POLICY "Members can delete pages" ON public.pages
  FOR DELETE TO authenticated
  USING (public.is_page_admin(auth.uid(), id));

-- ----------------------------------------------------------------------------
-- RLS: page_versions
-- SELECT/INSERT permitido para quem pode acessar a page; sem UPDATE/DELETE (imutavel)
-- ----------------------------------------------------------------------------
CREATE POLICY "Members can view page_versions" ON public.page_versions
  FOR SELECT TO authenticated
  USING (public.can_access_page(auth.uid(), page_id));

CREATE POLICY "Members can insert page_versions" ON public.page_versions
  FOR INSERT TO authenticated
  WITH CHECK (public.can_access_page(auth.uid(), page_id));

-- ----------------------------------------------------------------------------
-- RLS: page_permissions (espelha board_permissions com is_page_admin)
-- ----------------------------------------------------------------------------
CREATE POLICY "Members can view page_permissions" ON public.page_permissions
  FOR SELECT TO authenticated
  USING (public.can_access_page(auth.uid(), page_id));

CREATE POLICY "Page admins can insert page_permissions" ON public.page_permissions
  FOR INSERT TO authenticated
  WITH CHECK (public.is_page_admin(auth.uid(), page_id));

CREATE POLICY "Page admins can update page_permissions" ON public.page_permissions
  FOR UPDATE TO authenticated
  USING (public.is_page_admin(auth.uid(), page_id));

CREATE POLICY "Page admins can delete page_permissions" ON public.page_permissions
  FOR DELETE TO authenticated
  USING (public.is_page_admin(auth.uid(), page_id));

-- ----------------------------------------------------------------------------
-- Realtime publication
-- ----------------------------------------------------------------------------
ALTER PUBLICATION supabase_realtime ADD TABLE public.pages;
ALTER PUBLICATION supabase_realtime ADD TABLE public.page_versions;
ALTER PUBLICATION supabase_realtime ADD TABLE public.page_permissions;
```

Notas:
- NAO usar em-dash (regra do projeto). Usar hifen normal `-` quando necessario.
- Sem acentos em comentarios SQL (segue padrao do projeto).
- Trigger `update_updated_at` ja existe (definido em migrations anteriores). Se nao existir, criar:
  ```sql
  CREATE OR REPLACE FUNCTION public.update_updated_at() RETURNS TRIGGER LANGUAGE plpgsql AS $$ BEGIN NEW.updated_at = now(); RETURN NEW; END; $$;
  ```
  Conferir antes de adicionar duplicado.
</action>
<verify>
  <automated>cd /home/projects/hub-tarefas-lfpro && npx supabase db push --linked --dry-run 2>&1 | grep -E "pages_schema|ERROR" || echo "Migration valida sintaticamente"</automated>
</verify>
<done>Arquivo SQL existe, sintaxe valida, contem CREATE TABLE pages/page_versions/page_permissions, RPCs can_access_page/is_page_admin, todas as policies, ALTER PUBLICATION.</done>
</task>

<task id="2" type="auto">
<files>supabase/migrations/20260522110000_pages_schema.sql</files>
<action>
Aplicar a migration ao projeto Supabase remoto.

```bash
cd /home/projects/hub-tarefas-lfpro
npx supabase db push --linked
```

Se solicitar token: usar `ACCESS_TOKEN_SUPABASE` do `.env`. O project ref e `legvzsdbgyggubdomwxp` (config em `supabase/config.toml`).

Validar:
```bash
npx supabase db diff --linked --schema public 2>&1 | head -50
```
Deve mostrar zero diff apos push.
</action>
<verify>
  <automated>cd /home/projects/hub-tarefas-lfpro && npx supabase db diff --linked --schema public 2>&1 | grep -E "pages|page_versions|page_permissions" | head -5 || echo "Schema sincronizado"</automated>
</verify>
<done>Migration aplicada com sucesso ao projeto remoto. `supabase db diff --linked` retorna sem diferencas para as tabelas novas.</done>
</task>

<task id="3" type="auto">
<files>src/integrations/supabase/types.ts</files>
<action>
Regenerar tipos TypeScript do schema atualizado.

```bash
cd /home/projects/hub-tarefas-lfpro
npx supabase gen types typescript --linked > src/integrations/supabase/types.ts
```

Validar que o arquivo agora contem definicoes de:
- `pages: { Row: {...}, Insert: {...}, Update: {...} }`
- `page_versions: { Row: {...}, Insert: {...}, Update: {...} }`
- `page_permissions: { Row: {...}, Insert: {...}, Update: {...} }`
- Functions: `can_access_page`, `is_page_admin`

Conferir com grep:
```bash
grep -E "pages:|page_versions:|page_permissions:|can_access_page|is_page_admin" src/integrations/supabase/types.ts | head -10
```

Cuidado: NAO commitar quebras de TS em outros arquivos. Se o regen quebrar imports antigos, abrir issue, NAO mascarar com `as any`.
</action>
<verify>
  <automated>cd /home/projects/hub-tarefas-lfpro && grep -c "pages:\|page_versions:\|page_permissions:\|can_access_page\|is_page_admin" src/integrations/supabase/types.ts</automated>
</verify>
<done>Tipos regenerados. Grep retorna 5+ matches. `npm run build` continua passando.</done>
</task>

<task id="4" type="auto">
<files>src/types/page.ts</files>
<action>
Criar `src/types/page.ts` com as interfaces de dominio. Seguir padrao de `src/types/board.ts`. NAO usar `any`.

Conteudo literal:

```typescript
// Tipos de dominio para o modo Pagina (Docs Mode). Mantem paridade com Board.

export type PageRole = 'admin' | 'editor' | 'member' | 'viewer';

export type PageState = 'active' | 'archived' | 'deleted';

/**
 * Bloco BlockNote no formato JSON nativo (estrutura raiz armazenada em pages.content).
 * Mantemos como unknown[] pois o schema do BlockNote evolui; validacao no runtime
 * fica no editor (Plano 01-02).
 */
export type PageContent = unknown[];

export interface Page {
  id: string;
  workspace_id: string;
  folder_id: string | null;
  title: string;
  content: PageContent;
  state: PageState;
  icon: string | null;
  cover_url: string | null;
  position: number;
  created_by: string | null;
  created_at: string;
  updated_at: string;
}

export interface PageVersion {
  id: string;
  page_id: string;
  content: PageContent;
  title: string | null;
  created_by: string | null;
  created_at: string;
}

export interface PagePermission {
  id: string;
  page_id: string;
  user_id: string;
  role: PageRole;
  created_at: string;
}

/**
 * Tipo discriminado utilizado pelo Sidebar para listar boards + pages mesclados.
 * Plano 01-03 consome este tipo.
 */
export type WorkspaceEntry =
  | { kind: 'board'; id: string; name: string; icon: string | null; color: string | null; folder_id: string | null; position: number; workspace_id: string }
  | { kind: 'page'; id: string; title: string; icon: string | null; folder_id: string | null; position: number; workspace_id: string };
```

NAO importar de `@/integrations/supabase/types.ts` ainda (manter desacoplado por simplicidade — refletir tipos a mao).
</action>
<verify>
  <automated>cd /home/projects/hub-tarefas-lfpro && npx tsc --noEmit src/types/page.ts 2>&1 | grep -E "error" | head -5 || echo "OK"</automated>
</verify>
<done>Arquivo criado, sem erros de tipo, exporta `Page`, `PageVersion`, `PagePermission`, `PageRole`, `PageState`, `PageContent`, `WorkspaceEntry`.</done>
</task>

<task id="5" type="auto">
<files>src/test/page-types.test.ts</files>
<action>
Criar teste unitario minimo do tipo `WorkspaceEntry` (discriminated union) para garantir que o narrowing funciona. Padrao seguindo `src/test/example.test.ts`.

```typescript
import { describe, it, expect } from 'vitest';
import type { WorkspaceEntry } from '@/types/page';

describe('WorkspaceEntry discriminated union', () => {
  it('narrows board kind', () => {
    const entry: WorkspaceEntry = {
      kind: 'board',
      id: 'b1',
      name: 'Board 1',
      icon: null,
      color: null,
      folder_id: null,
      position: 0,
      workspace_id: 'ws1',
    };
    if (entry.kind === 'board') {
      expect(entry.name).toBe('Board 1');
    } else {
      throw new Error('narrowing failed');
    }
  });

  it('narrows page kind', () => {
    const entry: WorkspaceEntry = {
      kind: 'page',
      id: 'p1',
      title: 'Pagina 1',
      icon: null,
      folder_id: null,
      position: 0,
      workspace_id: 'ws1',
    };
    if (entry.kind === 'page') {
      expect(entry.title).toBe('Pagina 1');
    } else {
      throw new Error('narrowing failed');
    }
  });
});
```
</action>
<verify>
  <automated>cd /home/projects/hub-tarefas-lfpro && npx vitest run src/test/page-types.test.ts</automated>
</verify>
<done>Teste roda e passa.</done>
</task>

## Criterios de Sucesso

- [ ] Migration `20260522110000_pages_schema.sql` aplicada ao Supabase remoto
- [ ] Tabelas `pages`, `page_versions`, `page_permissions` existem com RLS
- [ ] RPCs `can_access_page` e `is_page_admin` existem e funcionam
- [ ] Realtime publication inclui as 3 tabelas
- [ ] `src/integrations/supabase/types.ts` regenerado, build ainda passa
- [ ] `src/types/page.ts` exporta interfaces de dominio
- [ ] Teste unitario do tipo discriminado passa
