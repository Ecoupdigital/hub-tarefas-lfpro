---
phase: 03-views-notion-style-com-toggle-de-estilo
plan: 03-01
type: feature
autonomous: true
wave: 0
depends_on: []
requirements: [REQ-21, REQ-22]
files_modified:
  - src/types/database.ts
  - src/styles/notion-theme.css
  - src/index.css
  - src/hooks/useViewStyle.ts
must_haves:
  truths:
    - "Tipo TS ViewStyle ('lfpro' | 'notion') existe e e exportado de src/types/database.ts"
    - "CSS notion-theme.css define paleta Notion (light + dark) sob a classe .notion-view"
    - "Hook useViewStyle(viewId) retorna { style, setStyle } persistindo em board_views.config.style"
  artifacts:
    - path: "src/types/database.ts"
      provides: "Tipo ViewStyle + constante VIEW_STYLE_DEFAULT + helper getViewStyle(view)"
    - path: "src/styles/notion-theme.css"
      provides: "Variaveis CSS Notion (--notion-bg, --notion-border, etc.) escopadas em .notion-view"
    - path: "src/hooks/useViewStyle.ts"
      provides: "Hook useViewStyle(viewId) retorna style atual + setter que chama useUpdateBoardViewConfig"
  key_links:
    - from: "useViewStyle.ts"
      to: "useBoardViews + useUpdateBoardViewConfig (src/hooks/useBoardViews.ts)"
      via: "Le data.config.style e chama mutation com config merged"
    - from: "src/index.css"
      to: "notion-theme.css"
      via: "@import './styles/notion-theme.css'"
---

# Fase 3 Plano 01: Foundation (tipos, paleta Notion, hook useViewStyle)

**Objetivo:** Estabelecer as fundacoes para os 4 NotionViews. Define o tipo `ViewStyle`, instala as variaveis CSS da paleta Notion (light + dark, escopadas em `.notion-view`), e cria o hook `useViewStyle(viewId)` que le/persiste `board_views.config.style`. Sem este plano, nenhum outro funciona — toggle nao salva, paleta cinza nao existe, Notion components nao tem theme.

**Cobre:** REQ-21 (toggle por view), REQ-22 (persistencia em board_views.config.style).

## Research Inline

**Stack atual (confirmado em codebase):**
- React 18 + TS 5.8 + Tailwind CSS 3
- shadcn-ui (Radix) — `Switch` ja existe em `src/components/ui/switch.tsx`
- React Query v5 — `useBoardViews` retorna `data: BoardView[]` com `config: jsonb`
- `useUpdateBoardViewConfig({ viewId, config })` ja existe em `src/hooks/useBoardViews.ts:87` e faz `supabase.from('board_views').update({ config: config as never })`

**Decisao chave:** A "migration" do CONTEXT.md (sem schema change, apenas convencao no jsonb `config`) significa que **nao escrevemos SQL**. Apenas leitura tipada em hook + setter mergeando o `style` no `config` existente.

**Paleta Notion (CONTEXT.md `<specifics>`):**
- Light: bg `#FFFFFF`, panel `#F7F6F3`, border `#E9E9E7`, text-primary `#37352F`, text-secondary `#787774`, blue accent `#2383E2`, hover-bg `#F1F1EF`
- Dark: bg `#191919`, panel `#202020`, border `#373737`, text-primary `#FFFFFF`, text-secondary `#A6A6A6`, blue accent `#529CCA`, hover-bg `#252525`
- Status accents (semantica Notion): vermelho `#E03E3E`, amarelo `#DFAB01`, verde `#0F7B6C`, azul `#0B6E99`, roxo `#6940A5`, cinza `#9B9A97`

**Tipografia Notion:** system sans (`-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif`). Nao Jost/Montserrat (estes ficam no LFPro).

## Contexto

@src/hooks/useBoardViews.ts — hook `useUpdateBoardViewConfig` (ja existe) + `useBoardViews`
@src/types/database.ts — onde ViewStyle deve ser exportado (junto com DatabaseViewType)
@src/index.css — onde fazer @import do notion-theme.css
@src/components/database/DatabaseViewRenderer.tsx — sera estendido em plano 02 pra usar useViewStyle

## Tarefas

<task id="1" type="auto">
<files>src/types/database.ts</files>
<action>
Adicionar ao final do arquivo (apos `DatabaseBoard`):

```typescript
// ── View style toggle (Fase 03) ────────────────────────────────────────────

/**
 * Estilo visual de uma view de database.
 * 'lfpro' (default): reusa Board* via mode='database', tokens warm gold LFPro.
 * 'notion': renderiza NotionTableView/Kanban/Calendar/List com paleta cinza neutra.
 *
 * Persistido em board_views.config.style (jsonb, default 'lfpro').
 */
export type ViewStyle = 'lfpro' | 'notion';

export const VIEW_STYLES: readonly ViewStyle[] = ['lfpro', 'notion'] as const;

export const VIEW_STYLE_DEFAULT: ViewStyle = 'lfpro';

export const VIEW_STYLE_LABELS: Record<ViewStyle, string> = {
  lfpro: 'LFPro',
  notion: 'Notion',
};

/**
 * Le o style de uma BoardView (defensivo: trata config null, style faltando, valor invalido).
 */
export function getViewStyle(view: { config?: Record<string, unknown> | null } | null | undefined): ViewStyle {
  const raw = (view?.config as Record<string, unknown> | null | undefined)?.style;
  if (raw === 'notion' || raw === 'lfpro') return raw;
  return VIEW_STYLE_DEFAULT;
}
```

NAO alterar nada existente no arquivo. Apenas adicionar ao final.
</action>
<verify><automated>npx tsc --noEmit 2>&1 | grep -E "src/types/database\.ts" || echo "OK no type errors"</automated></verify>
<done>Tipo ViewStyle, constantes VIEW_STYLES/VIEW_STYLE_DEFAULT/VIEW_STYLE_LABELS, helper getViewStyle exportados. tsc --noEmit passa sem novos erros no arquivo.</done>
</task>

<task id="2" type="auto">
<files>src/styles/notion-theme.css</files>
<action>
Criar arquivo novo (diretorio `src/styles/` ja existe — contem blocknote-overrides.css).

Conteudo exato:

```css
/* ===== Notion Theme Variables (Fase 03) ===== */
/* Escopado em .notion-view para nao interferir com tema LFPro global.
   Toda paleta cinza neutra, sem warm gold. Funciona em light/dark independente. */

.notion-view {
  /* Paleta light (default quando .notion-view nao esta dentro de .dark) */
  --notion-bg: #FFFFFF;
  --notion-panel: #F7F6F3;
  --notion-border: #E9E9E7;
  --notion-text-primary: #37352F;
  --notion-text-secondary: #787774;
  --notion-text-tertiary: #9B9A97;
  --notion-hover-bg: #F1F1EF;
  --notion-row-hover: #F7F6F3;
  --notion-header-bg: #FBFBFA;
  --notion-blue: #2383E2;
  --notion-blue-bg: rgba(35, 131, 226, 0.10);

  /* Status accents (semantica Notion) */
  --notion-status-red: #E03E3E;
  --notion-status-red-bg: rgba(224, 62, 62, 0.12);
  --notion-status-orange: #D9730D;
  --notion-status-orange-bg: rgba(217, 115, 13, 0.12);
  --notion-status-yellow: #DFAB01;
  --notion-status-yellow-bg: rgba(223, 171, 1, 0.14);
  --notion-status-green: #0F7B6C;
  --notion-status-green-bg: rgba(15, 123, 108, 0.14);
  --notion-status-blue: #0B6E99;
  --notion-status-blue-bg: rgba(11, 110, 153, 0.12);
  --notion-status-purple: #6940A5;
  --notion-status-purple-bg: rgba(105, 64, 165, 0.14);
  --notion-status-pink: #AD1A72;
  --notion-status-pink-bg: rgba(173, 26, 114, 0.12);
  --notion-status-gray: #9B9A97;
  --notion-status-gray-bg: rgba(155, 154, 151, 0.18);

  /* Tipografia: system sans Notion (nao Jost/Montserrat) */
  --notion-font: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;

  /* Tamanhos compactos Notion */
  --notion-row-h: 32px;
  --notion-kanban-card-padding: 8px 10px;
  --notion-list-row-h: 40px;
}

/* Dark mode: aplicado quando .notion-view esta dentro de .dark (raiz <html>) */
.dark .notion-view {
  --notion-bg: #191919;
  --notion-panel: #202020;
  --notion-border: #373737;
  --notion-text-primary: #FFFFFF;
  --notion-text-secondary: #A6A6A6;
  --notion-text-tertiary: #6B6B6B;
  --notion-hover-bg: #252525;
  --notion-row-hover: #202020;
  --notion-header-bg: #1F1F1F;
  --notion-blue: #529CCA;
  --notion-blue-bg: rgba(82, 156, 202, 0.16);

  --notion-status-red: #EF5350;
  --notion-status-red-bg: rgba(239, 83, 80, 0.18);
  --notion-status-orange: #FF9F40;
  --notion-status-orange-bg: rgba(255, 159, 64, 0.18);
  --notion-status-yellow: #FFDC49;
  --notion-status-yellow-bg: rgba(255, 220, 73, 0.18);
  --notion-status-green: #4DAB9A;
  --notion-status-green-bg: rgba(77, 171, 154, 0.20);
  --notion-status-blue: #529CCA;
  --notion-status-blue-bg: rgba(82, 156, 202, 0.20);
  --notion-status-purple: #9A6DD7;
  --notion-status-purple-bg: rgba(154, 109, 215, 0.20);
  --notion-status-pink: #E255A1;
  --notion-status-pink-bg: rgba(226, 85, 161, 0.20);
  --notion-status-gray: #A6A6A6;
  --notion-status-gray-bg: rgba(166, 166, 166, 0.18);
}

/* Helpers de utilidade dentro de .notion-view */
.notion-view {
  background-color: var(--notion-bg);
  color: var(--notion-text-primary);
  font-family: var(--notion-font);
}

.notion-view .notion-border { border-color: var(--notion-border); }
.notion-view .notion-hover:hover { background-color: var(--notion-hover-bg); }
.notion-view .notion-row-hover:hover { background-color: var(--notion-row-hover); }
.notion-view .notion-text-secondary { color: var(--notion-text-secondary); }
.notion-view .notion-text-tertiary { color: var(--notion-text-tertiary); }
.notion-view .notion-panel { background-color: var(--notion-panel); }
.notion-view .notion-header-bg { background-color: var(--notion-header-bg); }
```

Importante: NAO usar `:root` para essas variaveis. Tem que ser `.notion-view` para escopar e nao poluir o tema LFPro global.
</action>
<verify><automated>test -f src/styles/notion-theme.css && grep -q "\-\-notion-bg" src/styles/notion-theme.css && grep -q "\.dark \.notion-view" src/styles/notion-theme.css && echo OK</automated></verify>
<done>Arquivo src/styles/notion-theme.css existe, contem variaveis `--notion-*` escopadas em `.notion-view` (light e dark), inclui paleta status colors e tipografia system sans.</done>
</task>

<task id="3" type="auto">
<files>src/index.css</files>
<action>
Adicionar UMA linha de import logo apos o import existente de blocknote-overrides.css (linha 5).

Antes (linha 5):
```css
@import "./styles/blocknote-overrides.css";
```

Depois:
```css
@import "./styles/blocknote-overrides.css";
@import "./styles/notion-theme.css";
```

NAO alterar nada mais no arquivo. Apenas adicionar a linha.
</action>
<verify><automated>grep -q '@import "./styles/notion-theme.css"' src/index.css && echo OK</automated></verify>
<done>src/index.css importa notion-theme.css logo apos blocknote-overrides.css.</done>
</task>

<task id="4" type="auto">
<files>src/hooks/useViewStyle.ts</files>
<action>
Criar arquivo novo. Conteudo exato:

```typescript
import { useCallback } from 'react';
import { useBoardViews, useUpdateBoardViewConfig } from '@/hooks/useBoardViews';
import { getViewStyle, type ViewStyle, VIEW_STYLE_DEFAULT } from '@/types/database';

/**
 * Hook para ler/persistir o estilo visual (lfpro | notion) de uma board_view.
 *
 * Le board_views.config.style via useBoardViews(boardId). Setter merge `style`
 * no objeto config existente e chama useUpdateBoardViewConfig.
 *
 * @param boardId Id do board (para fetch de views via React Query cache compartilhado)
 * @param viewId  Id da view ativa. Se null/undefined, retorna VIEW_STYLE_DEFAULT e setter no-op.
 *
 * @example
 * const { style, setStyle, isUpdating } = useViewStyle(boardId, activeViewId);
 * <Switch checked={style === 'notion'} onCheckedChange={(c) => setStyle(c ? 'notion' : 'lfpro')} />
 */
export function useViewStyle(boardId: string | null, viewId: string | null | undefined) {
  const { data: views = [] } = useBoardViews(boardId);
  const updateConfig = useUpdateBoardViewConfig();

  const currentView = viewId ? views.find((v) => v.id === viewId) : undefined;
  const style: ViewStyle = getViewStyle(currentView ?? null);

  const setStyle = useCallback(
    (next: ViewStyle) => {
      if (!viewId || !currentView) return Promise.resolve();
      const baseConfig =
        (currentView.config as Record<string, unknown> | null | undefined) ?? {};
      const nextConfig = { ...baseConfig, style: next };
      return updateConfig.mutateAsync({ viewId, config: nextConfig });
    },
    [viewId, currentView, updateConfig]
  );

  return {
    style,
    setStyle,
    isUpdating: updateConfig.isPending,
    /** True quando a view existe e tem style explicito persistido (nao default). */
    isPersisted: !!(currentView?.config as Record<string, unknown> | null | undefined)?.style,
  };
}

export { VIEW_STYLE_DEFAULT };
```

Notas:
- Reusa hooks existentes (`useBoardViews`, `useUpdateBoardViewConfig`).
- Setter retorna Promise para callers que queiram await.
- `style` SEMPRE valido (fallback VIEW_STYLE_DEFAULT via getViewStyle).
</action>
<verify><automated>test -f src/hooks/useViewStyle.ts && npx tsc --noEmit 2>&1 | grep -E "src/hooks/useViewStyle\.ts" || echo OK</automated></verify>
<done>Hook useViewStyle exportado, le board_views.config.style via useBoardViews, persiste via useUpdateBoardViewConfig mergeando style no config existente. tsc passa.</done>
</task>

<task id="5" type="auto">
<files>src/test/useViewStyle.test.ts</files>
<action>
Criar teste unitario do helper `getViewStyle` (function pura, sem React). Vitest + import direto.

Conteudo:

```typescript
import { describe, it, expect } from 'vitest';
import { getViewStyle, VIEW_STYLE_DEFAULT } from '@/types/database';

describe('getViewStyle', () => {
  it('returns default lfpro when view is null', () => {
    expect(getViewStyle(null)).toBe('lfpro');
    expect(getViewStyle(undefined)).toBe('lfpro');
    expect(VIEW_STYLE_DEFAULT).toBe('lfpro');
  });

  it('returns default lfpro when config is null/undefined', () => {
    expect(getViewStyle({ config: null })).toBe('lfpro');
    expect(getViewStyle({ config: undefined })).toBe('lfpro');
    expect(getViewStyle({} as { config?: Record<string, unknown> })).toBe('lfpro');
  });

  it('returns default lfpro when config.style is missing', () => {
    expect(getViewStyle({ config: {} })).toBe('lfpro');
    expect(getViewStyle({ config: { other: 'value' } })).toBe('lfpro');
  });

  it('returns notion when config.style is notion', () => {
    expect(getViewStyle({ config: { style: 'notion' } })).toBe('notion');
  });

  it('returns lfpro when config.style is lfpro', () => {
    expect(getViewStyle({ config: { style: 'lfpro' } })).toBe('lfpro');
  });

  it('falls back to default on invalid style values', () => {
    expect(getViewStyle({ config: { style: 'invalid' } })).toBe('lfpro');
    expect(getViewStyle({ config: { style: 123 } })).toBe('lfpro');
    expect(getViewStyle({ config: { style: null } })).toBe('lfpro');
  });
});
```
</action>
<verify><automated>npx vitest run src/test/useViewStyle.test.ts 2>&1 | tail -5</automated></verify>
<done>6 testes de getViewStyle passam. Cobre null/undefined view, config null/missing, style invalido (fallback) e valores validos.</done>
</task>

## Criterios de Sucesso

- [ ] `import { ViewStyle, getViewStyle, VIEW_STYLE_DEFAULT } from '@/types/database'` funciona
- [ ] Variavel CSS `--notion-bg` resolve quando elemento esta dentro de `.notion-view`
- [ ] Hook `useViewStyle(boardId, viewId)` retorna `style` correto e `setStyle` persiste via mutation
- [ ] Teste de `getViewStyle` passa (vitest)
- [ ] tsc --noEmit nao introduz novos erros
- [ ] Nenhum arquivo existente alem de `src/index.css` (1 linha) foi modificado
