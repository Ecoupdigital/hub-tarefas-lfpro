---
phase: 03-views-notion-style-com-toggle-de-estilo
plan: 03-02
type: feature
autonomous: true
wave: 0
depends_on: [03-01]
requirements: [REQ-21, REQ-22, REQ-28]
files_modified:
  - src/components/database/ViewStyleToggle.tsx
  - src/components/database/DatabaseViewTabs.tsx
  - src/components/database/DatabaseViewRenderer.tsx
  - src/components/database/notion/.gitkeep
must_haves:
  truths:
    - "DatabaseViewTabs renderiza ViewStyleToggle a direita do nome da view ativa"
    - "Click no toggle persiste style em board_views.config.style via useViewStyle"
    - "DatabaseViewRenderer despacha para LFPro (BoardTable/Kanban/Calendar/DatabaseListView) quando style='lfpro' e para stub Notion* quando style='notion'"
    - "Estilo LFPro continua identico ao anterior (sem regressao visual)"
  artifacts:
    - path: "src/components/database/ViewStyleToggle.tsx"
      provides: "Componente switch LFPro/Notion conectado a useViewStyle"
    - path: "src/components/database/DatabaseViewRenderer.tsx"
      provides: "Renderer estendido que despacha por style + view_type"
    - path: "src/components/database/notion/"
      provides: "Diretorio onde Notion*View vivem (planos 03-03 ate 03-06)"
  key_links:
    - from: "DatabaseViewTabs"
      to: "ViewStyleToggle"
      via: "Render condicional quando ha activeViewId"
    - from: "ViewStyleToggle"
      to: "useViewStyle hook (03-01)"
      via: "style + setStyle"
    - from: "DatabaseViewRenderer"
      to: "BoardTable / NotionTableView (stub) etc."
      via: "switch(viewType + style)"
---

# Fase 3 Plano 02: ViewStyleToggle + DatabaseViewRenderer dispatching

**Objetivo:** Cria o componente UI do toggle (`ViewStyleToggle`), integra-o no header do `DatabaseViewTabs`, e estende `DatabaseViewRenderer` para despachar entre LFPro (Board* existentes) e Notion (componentes que serao criados nos planos 03-03 a 03-06; aqui usamos stubs minimos). Apos este plano, o usuario consegue alternar e ver pelo menos um placeholder Notion; estilo LFPro mantem 100% do comportamento anterior.

**Cobre:** REQ-21 (toggle visivel), REQ-22 (persistencia), REQ-28 (LFPro intocado / nao quebra).

## Research Inline

**shadcn Switch:** ja existe em `src/components/ui/switch.tsx` (componente Radix). Mas o CONTEXT.md especifica **"dois botoes pequenos lado a lado (LFPro / Notion), o ativo com fundo subtle"** — isto e um segmented control, NAO um switch on/off. Vamos usar `<button>` x2 com classes Tailwind condicionais (padrao do projeto, ver `DatabaseViewTabs.tsx:45` para mesma estrutura).

**DatabaseViewRenderer atual** (lido em codebase em `src/components/database/DatabaseViewRenderer.tsx`):
```tsx
{viewType === 'table' && <BoardTable mode="database" />}
{viewType === 'kanban' && <BoardKanban mode="database" />}
{viewType === 'calendar' && <BoardCalendar mode="database" />}
{viewType === 'list_detailed' && <DatabaseListView mode="database" activeViewId={activeView?.id ?? null} />}
```

**Decisao:** Extender renderer para `switch(viewType, style)`. Quando `style='notion'`, envolver em `<div className="notion-view">` para CSS variables aplicarem. Stubs minimos para Notion* views ate planos 03-03..06 substituirem.

## Contexto

@src/components/database/DatabaseViewRenderer.tsx — renderer atual a estender
@src/components/database/DatabaseViewTabs.tsx — tabs onde toggle vai injetado
@src/components/database/DatabaseBoardContext.tsx — wrapper de BoardProvider (mantido)
@src/hooks/useViewStyle.ts — criado em 03-01
@src/types/database.ts — ViewStyle types criados em 03-01

## Tarefas

<task id="1" type="auto">
<files>src/components/database/ViewStyleToggle.tsx</files>
<action>
Criar componente novo. Conteudo:

```tsx
import React from 'react';
import { cn } from '@/lib/utils';
import { useViewStyle } from '@/hooks/useViewStyle';
import { VIEW_STYLE_LABELS, type ViewStyle } from '@/types/database';

interface ViewStyleToggleProps {
  boardId: string;
  viewId: string | null | undefined;
  /**
   * Quando true, oculta o componente. Util quando nao ha view ativa.
   * Default: false.
   */
  disabled?: boolean;
}

/**
 * Segmented control LFPro / Notion para alternar o estilo visual da view ativa
 * da database. Persiste em board_views.config.style.
 *
 * UX:
 *  - Dois botoes lado a lado (LFPro / Notion), o ativo com bg-primary/15 text-primary
 *  - Os 2 botoes sao desabilitados durante mutation pra evitar clicks duplos
 *  - Tooltip nativo via title= (mantem MVP simples)
 *
 * Posicionado em DatabaseViewTabs apos as tabs, antes do botao "+".
 */
const ViewStyleToggle: React.FC<ViewStyleToggleProps> = ({ boardId, viewId, disabled }) => {
  const { style, setStyle, isUpdating } = useViewStyle(boardId, viewId);

  if (disabled || !viewId) return null;

  const options: ViewStyle[] = ['lfpro', 'notion'];

  return (
    <div
      role="group"
      aria-label="Estilo visual da view"
      className="flex items-center gap-0.5 ml-1 p-0.5 rounded-md bg-muted border border-border shrink-0"
    >
      {options.map((opt) => {
        const isActive = style === opt;
        return (
          <button
            key={opt}
            type="button"
            disabled={isUpdating || isActive}
            onClick={() => {
              if (isActive) return;
              setStyle(opt);
            }}
            className={cn(
              'px-2 py-0.5 rounded text-[11px] font-medium transition-colors whitespace-nowrap',
              isActive
                ? 'bg-background text-foreground shadow-sm'
                : 'text-muted-foreground hover:text-foreground'
            )}
            title={`Estilo ${VIEW_STYLE_LABELS[opt]}`}
            aria-pressed={isActive}
          >
            {VIEW_STYLE_LABELS[opt]}
          </button>
        );
      })}
    </div>
  );
};

export default ViewStyleToggle;
```

Notas:
- `bg-muted` usa token LFPro (cinza neutro de fundo do toggle), independente de estilo da view. Esta no header LFPro, nao na area Notion.
- Nao usa Radix Switch — segmented control via 2 <button> conforme CONTEXT.md.
- Strings em pt-BR ("Estilo LFPro", "Estilo Notion").
</action>
<verify><automated>test -f src/components/database/ViewStyleToggle.tsx && grep -q "useViewStyle" src/components/database/ViewStyleToggle.tsx && grep -q "aria-pressed" src/components/database/ViewStyleToggle.tsx && echo OK</automated></verify>
<done>Componente ViewStyleToggle exportado default. Le useViewStyle(boardId, viewId). Renderiza 2 botoes (LFPro / Notion) com estado ativo destacado. Persiste no click.</done>
</task>

<task id="2" type="auto">
<files>src/components/database/DatabaseViewTabs.tsx</files>
<action>
Modificar arquivo existente. Mudancas pontuais:

1. Adicionar import no topo (depois do import de CreateDatabaseViewDialog):
```tsx
import ViewStyleToggle from './ViewStyleToggle';
```

2. Logo apos o `.map((v) => { ... })` que renderiza as tabs (apos linha `})}` da iteracao de views), e ANTES do botao com `title="Nova view"`, INSERIR:

```tsx
{/* Toggle de estilo visual da view ativa (Fase 03). Posicionado entre as
    tabs e o botao "+ Nova view" conforme CONTEXT.md. So aparece quando ha
    uma view ativa (ViewStyleToggle ja faz disabled internamente). */}
<ViewStyleToggle boardId={boardId} viewId={activeViewId} />
```

NAO alterar mais nada do arquivo. A ordem visual final fica: tabs ... ViewStyleToggle ... botao "+". Manter classe `flex items-center gap-1 px-2 py-1 ...` no container exterior.

Verificar visualmente que o JSX gerado ainda compila (sem espacos quebrando JSX).
</action>
<verify><automated>grep -q "import ViewStyleToggle" src/components/database/DatabaseViewTabs.tsx && grep -q "<ViewStyleToggle boardId={boardId}" src/components/database/DatabaseViewTabs.tsx && npx tsc --noEmit 2>&1 | grep -E "DatabaseViewTabs" || echo OK</automated></verify>
<done>DatabaseViewTabs renderiza ViewStyleToggle entre as tabs e o botao "+". Sem regressao no comportamento de tabs.</done>
</task>

<task id="3" type="auto">
<files>src/components/database/notion/.gitkeep</files>
<action>
Criar arquivo vazio `.gitkeep` para reservar o diretorio `src/components/database/notion/`. Os componentes Notion*View viverao aqui (planos 03-03 ate 03-06).

```bash
mkdir -p src/components/database/notion
touch src/components/database/notion/.gitkeep
```

Conteudo do `.gitkeep`: vazio (zero bytes).
</action>
<verify><automated>test -d src/components/database/notion && test -f src/components/database/notion/.gitkeep && echo OK</automated></verify>
<done>Diretorio src/components/database/notion/ existe com .gitkeep vazio.</done>
</task>

<task id="4" type="auto">
<files>src/components/database/notion/NotionTableView.tsx</files>
<action>
Criar **stub** minimo de NotionTableView. Sera substituido pelo plano 03-03 com a implementacao real. Stub serve para o renderer (tarefa 5) poder importar.

Conteudo:

```tsx
import React from 'react';
import { useApp } from '@/context/AppContext';

interface NotionTableViewProps {
  mode?: 'database' | 'board';
}

/**
 * STUB Fase 03 plano 02 — substituido por implementacao real no plano 03-03.
 *
 * Apenas exibe placeholder dizendo "Notion Table view em construcao" para que
 * o switch no DatabaseViewRenderer compile e a infraestrutura de toggle possa
 * ser validada antes de implementar a view completa.
 */
const NotionTableView: React.FC<NotionTableViewProps> = ({ mode = 'database' }) => {
  const { activeBoard } = useApp();

  return (
    <div className="p-6 text-center text-sm notion-text-secondary">
      <p className="font-medium notion-text-primary mb-1">Notion Table View</p>
      <p>Em construcao (plano 03-03).</p>
      {activeBoard && (
        <p className="mt-2 text-[11px] notion-text-tertiary">
          Board: {activeBoard.name} ({mode})
        </p>
      )}
    </div>
  );
};

export default NotionTableView;
```
</action>
<verify><automated>test -f src/components/database/notion/NotionTableView.tsx && grep -q "NotionTableView" src/components/database/notion/NotionTableView.tsx && echo OK</automated></verify>
<done>Stub NotionTableView default-exportado.</done>
</task>

<task id="5" type="auto">
<files>src/components/database/notion/NotionKanbanView.tsx</files>
<action>
Stub identico em estrutura ao NotionTableView, ajustando nome:

```tsx
import React from 'react';
import { useApp } from '@/context/AppContext';

interface NotionKanbanViewProps {
  mode?: 'database' | 'board';
}

/** STUB Fase 03 plano 02 — substituido pelo plano 03-04. */
const NotionKanbanView: React.FC<NotionKanbanViewProps> = ({ mode = 'database' }) => {
  const { activeBoard } = useApp();
  return (
    <div className="p-6 text-center text-sm notion-text-secondary">
      <p className="font-medium notion-text-primary mb-1">Notion Kanban View</p>
      <p>Em construcao (plano 03-04).</p>
      {activeBoard && (
        <p className="mt-2 text-[11px] notion-text-tertiary">Board: {activeBoard.name} ({mode})</p>
      )}
    </div>
  );
};

export default NotionKanbanView;
```

Mesma estrutura para NotionCalendarView.tsx e NotionListView.tsx, ajustando nome e plano referente (03-05, 03-06).

Criar os 3 arquivos no mesmo passo (stubs):
- `src/components/database/notion/NotionKanbanView.tsx`
- `src/components/database/notion/NotionCalendarView.tsx` (referenciar plano 03-05)
- `src/components/database/notion/NotionListView.tsx` (referenciar plano 03-06)
</action>
<verify><automated>for f in NotionKanbanView NotionCalendarView NotionListView; do test -f src/components/database/notion/$f.tsx || { echo MISSING $f; exit 1; }; done && echo OK</automated></verify>
<done>3 stubs criados: NotionKanbanView, NotionCalendarView, NotionListView. Cada um default-exporta um placeholder.</done>
</task>

<task id="6" type="auto">
<files>src/components/database/DatabaseViewRenderer.tsx</files>
<action>
**Reescrever** arquivo completo. Mudancas:

1. Importar `getViewStyle` de `@/types/database` e os 4 stubs Notion.
2. Calcular `style` da view ativa via `getViewStyle(activeView)`.
3. Switch despacha por `(viewType, style)`. Quando `style='notion'`, envolver children em `<div className="notion-view">` para que as CSS variables aplicarem.

Conteudo completo do arquivo:

```tsx
import React from 'react';
import { useBoardViews } from '@/hooks/useBoardViews';
import DatabaseBoardContext from './DatabaseBoardContext';
import BoardTable from '@/components/board/BoardTable';
import BoardKanban from '@/components/board/BoardKanban';
import BoardCalendar from '@/components/board/BoardCalendar';
import DatabaseListView from './DatabaseListView';
import NotionTableView from './notion/NotionTableView';
import NotionKanbanView from './notion/NotionKanbanView';
import NotionCalendarView from './notion/NotionCalendarView';
import NotionListView from './notion/NotionListView';
import { getViewStyle } from '@/types/database';
import type { DatabaseViewType } from '@/types/database';

interface Props {
  boardId: string;
  /**
   * Id da view ativa (vem de DatabaseViewTabs no plano 02-08).
   * Se null/undefined, usa is_default ou primeira view.
   */
  activeViewId?: string | null;
}

/**
 * Renderer da view ativa da database inline.
 *
 * Envolve BoardTable/Kanban/Calendar com `DatabaseBoardContext` (que injeta
 * `boardIdOverride` no BoardProvider local), garantindo que cada bloco
 * database renderize seu proprio board sem alterar o board ativo do app.
 *
 * O switch entre views (table/kanban/calendar/list_detailed) e determinado
 * por `board_views.view_type` da view ativa.
 *
 * Fase 03: adicionalmente despacha por `board_views.config.style` ('lfpro'|'notion').
 * Quando style='notion', renderiza componentes Notion* dentro de `<div className="notion-view">`
 * para escopar a paleta cinza neutra (variaveis CSS em src/styles/notion-theme.css).
 */
const DatabaseViewRenderer: React.FC<Props> = ({ boardId, activeViewId }) => {
  const { data: views = [], isLoading } = useBoardViews(boardId);

  if (isLoading) {
    return (
      <div className="px-3 py-4 font-density-cell text-muted-foreground">
        Carregando views...
      </div>
    );
  }

  const activeView = activeViewId
    ? views.find((v) => v.id === activeViewId)
    : views.find((v) => v.is_default) ?? views[0];

  const viewType: DatabaseViewType = (activeView?.view_type as DatabaseViewType) ?? 'table';
  const style = getViewStyle(activeView ?? null);

  const lfproContent = (
    <>
      {viewType === 'table' && <BoardTable mode="database" />}
      {viewType === 'kanban' && <BoardKanban mode="database" />}
      {viewType === 'calendar' && <BoardCalendar mode="database" />}
      {viewType === 'list_detailed' && (
        <DatabaseListView mode="database" activeViewId={activeView?.id ?? null} />
      )}
    </>
  );

  const notionContent = (
    <div className="notion-view">
      {viewType === 'table' && <NotionTableView mode="database" />}
      {viewType === 'kanban' && <NotionKanbanView mode="database" />}
      {viewType === 'calendar' && <NotionCalendarView mode="database" />}
      {viewType === 'list_detailed' && <NotionListView mode="database" />}
    </div>
  );

  return (
    <DatabaseBoardContext boardId={boardId}>
      {style === 'notion' ? notionContent : lfproContent}
    </DatabaseBoardContext>
  );
};

export default DatabaseViewRenderer;
```

NAO alterar nada em DatabaseBoardContext.tsx — wrapper continua identico.
</action>
<verify><automated>grep -q "getViewStyle" src/components/database/DatabaseViewRenderer.tsx && grep -q "className=\"notion-view\"" src/components/database/DatabaseViewRenderer.tsx && npx tsc --noEmit 2>&1 | grep -E "DatabaseViewRenderer" || echo OK</automated></verify>
<done>DatabaseViewRenderer despacha por (viewType, style). Notion content envolvido em `.notion-view`. LFPro content nao envolvido (mantem tokens warm gold globais). tsc passa.</done>
</task>

<task id="7" type="checkpoint:human-verify">
<files>none</files>
<action>
**Smoke test manual** apos as tarefas 1-6 estarem implementadas:

1. Rodar `npm run dev` na porta 8080
2. Abrir uma page com bloco database existente
3. Clicar no ViewStyleToggle alternando para 'Notion'
4. Verificar:
   - [ ] Botao 'Notion' fica destacado, 'LFPro' some o destaque
   - [ ] View ativa renderiza placeholder "Notion ... em construcao" (stub)
   - [ ] Toggling de volta pra 'LFPro' restaura a view original (BoardTable/Kanban/etc.)
   - [ ] Refresh da pagina mantem o estilo escolhido (persistencia funciona)
   - [ ] Em outras views (tabs distintas), toggle e independente (cada view tem seu style)
5. Abrir DevTools > inspecionar `<div className="notion-view">` e confirmar que `getComputedStyle` resolve `--notion-bg` para `#FFFFFF` (light) ou `#191919` (dark).

Reportar pass/fail. Em caso de fail, identificar qual tarefa anterior precisa fix.
</action>
<verify><automated>FALTANDO — smoke test manual; nao automatizavel em CI</automated></verify>
<done>Toggle funciona, persiste, paleta Notion aplica via CSS vars, LFPro views continuam intactas.</done>
</task>

## Criterios de Sucesso

- [ ] Toggle LFPro/Notion aparece a direita das tabs em DatabaseViewTabs
- [ ] Click no toggle persiste no banco (verificar `board_views.config.style` no Supabase)
- [ ] Renderer despacha corretamente para LFPro (4 views originais) e Notion (4 stubs)
- [ ] `<div className="notion-view">` envolve content Notion e variaveis CSS aplicam
- [ ] Estilo LFPro continua identico (regressao zero)
- [ ] Cada view tem seu style independente (toggle em tab A nao afeta tab B)
- [ ] Persistencia sobrevive a refresh
