---
phase: 03-views-notion-style-com-toggle-de-estilo
plan: 03-07
type: feature
autonomous: true
wave: 2
depends_on: [03-03b, 03-04, 03-05, 03-06]
requirements: [REQ-27, REQ-28]
files_modified:
  - src/test/notion-views.integration.test.ts
  - src/test/notion-paleta.test.ts
  - src/components/database/notion/NotionTableView.tsx
  - src/components/database/notion/NotionKanbanView.tsx
  - src/components/database/notion/NotionCalendarView.tsx
  - src/components/database/notion/NotionListView.tsx
must_haves:
  truths:
    - "Testes confirmam que ao alternar estilo nenhuma escrita extra acontece no banco (apenas re-render)"
    - "Testes confirmam que tokens warm gold (--primary, font-heading, font-sans Jost) NAO aparecem nos arquivos Notion*View"
    - "Visual review confirma que LFPro continua identico em todas as 4 views apos a fase"
    - "Documentacao README das views Notion documenta limitacoes do MVP (drag-calendar=no, people-edit=no, sort/filter=heredados do LFPro)"
  artifacts:
    - path: "src/test/notion-paleta.test.ts"
      provides: "Teste que faz grep em src/components/database/notion/*.tsx confirmando ausencia de tokens LFPro"
    - path: "src/test/notion-views.integration.test.ts"
      provides: "Testes de integracao: renderizar cada Notion*View com mock minimal de BoardContext, validar nao quebra"
    - path: "src/components/database/notion/README.md (opcional)"
      provides: "Documentacao para devs novos sobre a estrutura da fase 03"
  key_links:
    - from: "notion-paleta.test.ts"
      to: "src/components/database/notion/*.tsx"
      via: "readFileSync + regex assertions"
---

# Fase 3 Plano 07: Hardening + testes de isolamento + smoke final

**Objetivo:** Fechar a fase com (1) testes que travem regressao (paleta cinza pura, sem warm gold), (2) testes de integracao smoke que confirmem que NotionView render sem crash com BoardContext mockado, (3) ajustes finais identificados nos smoke tests dos planos 03-03 a 06.

**Cobre:** REQ-27 (paleta cinza confirmada por teste), REQ-28 (LFPro intacto confirmado).

## Research Inline

**Vitest + Testing Library:** `@testing-library/react` ja em deps. Padrao em `src/test/work-components.test.ts` (referencia).

**Estrategia de mock para Notion*View:**
- BoardContext fornece `activeBoard`. Mockar via `vi.mock('@/context/AppContext', () => ({ useApp: () => ({ activeBoard: fakeBoard, setSelectedItem: vi.fn() }) }))`.
- Hooks de mutation (`useUpdateColumnValue`, `useCreateItem`) mockar para retornar `{ mutate: vi.fn(), mutateAsync: vi.fn() }`.
- `useProfiles`, `useBoardViews` mockar via `vi.mock('@/hooks/useSupabaseData', () => ({ useProfiles: () => ({ data: [] }), useUpdateColumnValue: () => ({ mutate: vi.fn() }), useCreateItem: () => ({ mutate: vi.fn() }), useUpdateItem: () => ({ mutate: vi.fn() }) }))`.

## Contexto

@src/test/work-components.test.ts — referencia de mock de hooks
@src/components/database/notion/*.tsx — arquivos a verificar

## Tarefas

<task id="1" type="auto">
<files>src/test/notion-paleta.test.ts</files>
<action>
Teste que faz grep nos arquivos Notion* para garantir paleta cinza pura.

```typescript
import { describe, it, expect } from 'vitest';
import { readFileSync, readdirSync } from 'node:fs';
import { join } from 'node:path';

const NOTION_DIR = join(process.cwd(), 'src/components/database/notion');

/**
 * Lista de tokens LFPro proibidos em arquivos Notion*View.
 * Garante paleta cinza pura, sem warm gold ou font heading LFPro.
 */
const FORBIDDEN_TOKENS = [
  // Cor primaria LFPro (warm gold)
  'bg-primary',
  'text-primary',
  'border-primary',
  'ring-primary',
  // Fontes LFPro
  'font-heading',
  // Tokens warm gold custom
  'warm-gold',
  // CSS variables LFPro de board que viraram warm gold
  '--primary',
];

/**
 * Excecoes: trechos onde o termo aparece mas e legitimo (ex: comentarios).
 * Adicionar com sufixo regex se necessario.
 */
const ALLOWED_OCCURRENCES = new Set<string>([
  // Exemplo: 'foo bar' indica que essa linha pode conter token proibido por razao especifica
]);

function listTsxFiles(dir: string): string[] {
  return readdirSync(dir)
    .filter((f) => f.endsWith('.tsx') || f.endsWith('.ts'))
    .filter((f) => f !== '.gitkeep')
    .map((f) => join(dir, f));
}

describe('Notion views — paleta cinza pura (Fase 03 REQ-27)', () => {
  const files = listTsxFiles(NOTION_DIR);

  it('lista arquivos Notion* (sanity check)', () => {
    expect(files.length).toBeGreaterThan(0);
    expect(files.some((f) => f.endsWith('NotionTableView.tsx'))).toBe(true);
    expect(files.some((f) => f.endsWith('NotionKanbanView.tsx'))).toBe(true);
    expect(files.some((f) => f.endsWith('NotionCalendarView.tsx'))).toBe(true);
    expect(files.some((f) => f.endsWith('NotionListView.tsx'))).toBe(true);
  });

  it('NAO usa tokens warm gold LFPro (bg-primary, text-primary, font-heading, etc.)', () => {
    const violations: { file: string; token: string; line: number; text: string }[] = [];
    files.forEach((file) => {
      const content = readFileSync(file, 'utf-8');
      const lines = content.split('\n');
      lines.forEach((text, idx) => {
        // Ignora linhas de comentario
        const stripped = text.trim();
        if (stripped.startsWith('//') || stripped.startsWith('*') || stripped.startsWith('/*')) return;

        FORBIDDEN_TOKENS.forEach((token) => {
          if (text.includes(token)) {
            const occurrence = `${file}:${idx + 1}:${token}`;
            if (!ALLOWED_OCCURRENCES.has(occurrence)) {
              violations.push({ file, token, line: idx + 1, text: text.trim() });
            }
          }
        });
      });
    });

    if (violations.length > 0) {
      const summary = violations
        .map((v) => `${v.file}:${v.line} usa '${v.token}' em: ${v.text.slice(0, 80)}`)
        .join('\n');
      throw new Error(`Tokens LFPro proibidos detectados em arquivos Notion:\n${summary}`);
    }
    expect(violations).toEqual([]);
  });

  it('USA variaveis CSS --notion-* (paleta cinza)', () => {
    // Pelo menos um arquivo deve usar variavel Notion (sanity check)
    const allContent = files.map((f) => readFileSync(f, 'utf-8')).join('\n');
    expect(allContent).toMatch(/--notion-(bg|border|text-primary|text-secondary|panel)/);
  });
});
```
</action>
<verify><automated>npx vitest run src/test/notion-paleta.test.ts 2>&1 | tail -10</automated></verify>
<done>Teste de paleta passa. Lista todos os arquivos Notion*, falha se encontrar tokens LFPro (bg-primary, font-heading, etc.), confirma uso de --notion-*.</done>
</task>

<task id="2" type="auto">
<files>src/test/notion-views.integration.test.ts</files>
<action>
Smoke test de render para cada Notion*View. Garante que com mock minimo de BoardContext as 4 views renderizam sem crash.

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { render, screen, cleanup } from '@testing-library/react';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import React from 'react';
import type { Board } from '@/types/board';

// Mocks dos hooks de dados — todos retornam estado minimo
vi.mock('@/hooks/useSupabaseData', () => ({
  useProfiles: () => ({ data: [{ id: 'u1', name: 'Alice', email: 'a@a.com', avatar_url: null }] }),
  useUpdateColumnValue: () => ({ mutate: vi.fn(), mutateAsync: vi.fn(), isPending: false }),
  useCreateItem: () => ({ mutate: vi.fn(), mutateAsync: vi.fn(), isPending: false }),
  useUpdateItem: () => ({ mutate: vi.fn(), mutateAsync: vi.fn(), isPending: false }),
}));

vi.mock('@/hooks/useBoardViews', () => ({
  useBoardViews: () => ({ data: [], isLoading: false }),
  useUpdateBoardViewConfig: () => ({ mutate: vi.fn(), mutateAsync: vi.fn(), isPending: false }),
}));

const fakeBoard: Board = {
  id: 'b1',
  workspaceId: 'w1',
  name: 'DB Test',
  position: 0,
  page_id: 'p1',
  groups: [
    {
      id: 'g1',
      boardId: 'b1',
      title: 'Grupo 1',
      color: 'gray',
      isCollapsed: false,
      position: 0,
      items: [
        {
          id: 'i1',
          boardId: 'b1',
          groupId: 'g1',
          name: 'Item Alpha',
          position: 0,
          columnValues: {
            'c-status': { value: 'todo' } as any,
            'c-date': { value: '2026-05-22' } as any,
            'c-people': { value: ['u1'] } as any,
          },
        },
      ],
    },
  ],
  columns: [
    {
      id: 'c-status',
      boardId: 'b1',
      title: 'Status',
      type: 'status',
      width: 120,
      position: 0,
      settings: { labels: { todo: { name: 'A fazer', color: 'blue' }, done: { name: 'Feito', color: 'green' } } },
    },
    { id: 'c-date', boardId: 'b1', title: 'Data', type: 'date', width: 120, position: 1, settings: {} },
    { id: 'c-people', boardId: 'b1', title: 'Pessoas', type: 'people', width: 120, position: 2, settings: {} },
  ],
};

vi.mock('@/context/AppContext', () => ({
  useApp: () => ({
    activeBoard: fakeBoard,
    setSelectedItem: vi.fn(),
  }),
}));

function renderWithProviders(node: React.ReactElement) {
  const qc = new QueryClient({ defaultOptions: { queries: { retry: false } } });
  return render(<QueryClientProvider client={qc}>{node}</QueryClientProvider>);
}

describe('Notion views — smoke integration (Fase 03)', () => {
  beforeEach(() => cleanup());

  it('NotionTableView renderiza item Alpha sem crash', async () => {
    const NotionTableView = (await import('@/components/database/notion/NotionTableView')).default;
    renderWithProviders(<NotionTableView mode="database" />);
    expect(screen.getByText('Item Alpha')).toBeInTheDocument();
    expect(screen.getByText('Nome')).toBeInTheDocument(); // header
  });

  it('NotionKanbanView agrupa por status e mostra coluna A fazer com 1 card', async () => {
    const NotionKanbanView = (await import('@/components/database/notion/NotionKanbanView')).default;
    renderWithProviders(<NotionKanbanView mode="database" />);
    expect(screen.getByText('A fazer')).toBeInTheDocument();
    expect(screen.getByText('Item Alpha')).toBeInTheDocument();
    // Contador (1) presente
    expect(screen.getByText('(1)')).toBeInTheDocument();
  });

  it('NotionCalendarView renderiza header com mes/ano e dia 22 do mes', async () => {
    const NotionCalendarView = (await import('@/components/database/notion/NotionCalendarView')).default;
    renderWithProviders(<NotionCalendarView mode="database" />);
    // Header tem botoes Hoje, <, >
    expect(screen.getByRole('button', { name: /Hoje/i })).toBeInTheDocument();
    expect(screen.getByRole('button', { name: /Anterior/i })).toBeInTheDocument();
    // Dia 22 aparece como texto em alguma celula
    expect(screen.getAllByText(/22/).length).toBeGreaterThan(0);
  });

  it('NotionListView renderiza contador "1 item" e o item Alpha', async () => {
    const NotionListView = (await import('@/components/database/notion/NotionListView')).default;
    renderWithProviders(<NotionListView mode="database" />);
    expect(screen.getByText('Item Alpha')).toBeInTheDocument();
    expect(screen.getByText(/1 item/i)).toBeInTheDocument();
  });
});
```

Notas:
- Imports dinamicos `import('...')` permitem que os `vi.mock` aplicados no topo afetem as resolucoes.
- Sem assertions de drag (dnd-kit precisaria de setup pesado, fora do escopo do smoke).
- Mock de `useBoardViews` retorna `[]` — Notion*View deve cair em defaults (1a coluna de cada tipo).
</action>
<verify><automated>npx vitest run src/test/notion-views.integration.test.ts 2>&1 | tail -10</automated></verify>
<done>4 smoke tests passam, cada Notion*View renderiza minimo (sem crash) com BoardContext mockado.</done>
</task>

<task id="3" type="auto">
<files>src/components/database/notion/NotionTableView.tsx src/components/database/notion/NotionKanbanView.tsx src/components/database/notion/NotionCalendarView.tsx src/components/database/notion/NotionListView.tsx</files>
<action>
Apos rodar testes (tarefas 1 e 2), verificar saida do `notion-paleta.test.ts`. Se algum arquivo Notion* introduziu por engano `bg-primary`, `text-primary`, `font-heading`, etc., remover.

Acoes esperadas:
- Substituir qualquer ocorrencia de `bg-primary/15` por `notion-panel` (ou `style={{ backgroundColor: 'var(--notion-blue-bg)' }}` se semantica era "blue Notion")
- Substituir `text-primary` por `style={{ color: 'var(--notion-text-primary)' }}`
- Substituir `font-heading` por `style={{ fontFamily: 'var(--notion-font)' }}` (mas em geral `font-heading` nao deve aparecer; e Montserrat LFPro)
- Substituir `bg-warm-gold` se houver por `notion-panel`

Se `notion-paleta.test.ts` ja passa sem mudancas (porque os planos 03-03..06 ja seguem a paleta), pular esta tarefa.

Output esperado: tarefa idempotente. Se passa, marca como done.
</action>
<verify><automated>npx vitest run src/test/notion-paleta.test.ts 2>&1 | tail -5</automated></verify>
<done>notion-paleta.test.ts passa sem violacoes. Nenhum token LFPro warm gold em arquivos Notion*.</done>
</task>

<task id="4" type="auto">
<files>src/components/database/notion/README.md</files>
<action>
Documentacao curta para devs novos sobre a estrutura da fase 03.

```markdown
# Notion-style Database Views (Fase 03)

Esta pasta contem as 4 views Notion-style das databases inline, criadas na Fase 03 do roadmap.

## Estrutura

- `NotionTableView.tsx` — tabela compacta com edit inline (REQ-23)
- `NotionKanbanView.tsx` — kanban com cards limpos + drag entre status (REQ-24)
- `NotionCalendarView.tsx` — grid mes/semana com eventos coloridos (REQ-25)
- `NotionListView.tsx` — lista compacta horizontal (REQ-26)

## Helpers compartilhados

- `notionInlineCell.tsx` — primitivos visuais: `StatusPill`, `PersonAvatar`, `NotionInlineCell` (editor inline por tipo)
- `notionColumnIcon.tsx` — `NotionColumnIcon` (lucide icon por ColumnType)
- `useCalendarItems.ts` — hook que agrupa items por data
- (em `src/hooks/`) `useKanbanStatusGroup.ts` — agrupa por status
- (em `src/hooks/`) `useViewStyle.ts` — le/persiste `board_views.config.style`

## Paleta

Definida em `src/styles/notion-theme.css`, escopada em `.notion-view`. Carregada via
`<div className="notion-view">` injetado por `DatabaseViewRenderer` quando
`board_views.config.style === 'notion'`.

**Variaveis principais:**
- `--notion-bg`, `--notion-panel`, `--notion-border`
- `--notion-text-primary`, `--notion-text-secondary`, `--notion-text-tertiary`
- `--notion-blue` (accent), `--notion-blue-bg`
- `--notion-status-{red,orange,yellow,green,blue,purple,pink,gray}` + `-bg` variants

## Limitacoes do MVP (escopo Fase 03)

- **Calendar:** sem drag-drop de eventos entre dias (planejado para fase futura)
- **People:** read-only em todas as views Notion (edicao via `ItemDetailPanel`)
- **Sort / Filter / GroupBy:** nao implementado nas Notion views (LFPro tem; Notion seguira em fase futura)
- **Kanban:** sem reorder dentro da mesma coluna (so muda status)
- **Toggle Mes/Semana** no Calendar (sem Day/Year mode)
- **Subitems:** nao renderizados nas Notion views (so na tabela LFPro)

## Reuso

Notion views NAO duplicam logica de dados. Usam os mesmos hooks:
- `useApp().activeBoard` (via BoardProvider injetado por `DatabaseBoardContext`)
- `useProfiles()`, `useUpdateColumnValue()`, `useCreateItem()`, `useUpdateItem()` de `@/hooks/useSupabaseData`

## Adicionando uma nova Notion view (futuro)

1. Criar componente em `src/components/database/notion/Notion<Tipo>View.tsx`
2. Adicionar import + switch case em `src/components/database/DatabaseViewRenderer.tsx`
3. Estender `DatabaseViewType` em `src/types/database.ts` se for um tipo novo
4. Adicionar smoke test em `src/test/notion-views.integration.test.ts`

## Testes

- `src/test/useViewStyle.test.ts` — helpers de leitura/persistencia do style
- `src/test/notion-paleta.test.ts` — guard de paleta cinza pura (REQ-27)
- `src/test/notion-views.integration.test.ts` — smoke tests de render (REQ-23..26)
```
</action>
<verify><automated>test -f src/components/database/notion/README.md && grep -q "Fase 03" src/components/database/notion/README.md && echo OK</automated></verify>
<done>README documenta a estrutura da fase 03, limitacoes do MVP, padrao de reuso.</done>
</task>

<task id="5" type="checkpoint:human-verify">
<files>none</files>
<action>
**Verificacao final** (regressao + UX):

1. Rodar `npm run test` — todos os testes verdes (incluindo os 3 novos: useViewStyle, notion-paleta, notion-views.integration)
2. Rodar `npm run build` — build de producao passa sem erro
3. Smoke E2E manual:
   - Abrir page com database
   - Em cada view (Tabela, Kanban, Calendar, Lista):
     - Alternar LFPro → Notion → LFPro
     - Confirmar que LFPro continua identico ao pre-fase 3
     - Confirmar que Notion mostra paleta cinza pura (zero warm gold)
   - Em outra aba do navegador, abrir mesma page → confirmar que o style escolhido persiste
   - Em um workspace com 2+ databases, confirmar que toggle e independente por view
4. Reportar:
   - [ ] Testes verdes
   - [ ] Build limpo
   - [ ] LFPro intacto em todas as 4 views (REQ-28)
   - [ ] Notion paleta correta em todas as 4 views (REQ-27)
   - [ ] Persistencia ok
   - [ ] Independencia por view ok
</action>
<verify><automated>npm run test 2>&1 | tail -20 && npm run build 2>&1 | tail -10</automated></verify>
<done>Suite verde, build limpo, LFPro intacto, Notion paleta correta, persistencia confirmada.</done>
</task>

## Criterios de Sucesso

- [ ] Teste `notion-paleta` confirma ausencia de tokens LFPro nos arquivos Notion*
- [ ] Teste `notion-views.integration` confirma que 4 Notion*View renderizam sem crash
- [ ] README documenta limitacoes do MVP
- [ ] `npm run test` verde
- [ ] `npm run build` verde
- [ ] LFPro views identicas ao pre-fase (regressao zero)
- [ ] Persistencia em board_views.config.style validada manualmente
