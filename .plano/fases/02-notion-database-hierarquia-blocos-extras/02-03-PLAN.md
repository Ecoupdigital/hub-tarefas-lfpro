---
phase: 02-notion-database-hierarquia-blocos-extras
plan: 02-03
type: feature
autonomous: true
wave: 2
depends_on: [02-01, 02-02]
requirements: [REQ-11]
must_haves:
  truths:
    - "Drag/drop reordena pages entre irmaos via lexorank (fractional-indexing)"
    - "Drag/drop reanninha page (drop sobre outra page = vira filho dela)"
    - "Drop em database (boards com page_id) e rejeitado com feedback visual"
    - "Drop entre workspaces e rejeitado (so dentro do mesmo workspace no MVP)"
    - "Persistencia: sort_order escrita no banco apos drop end; UI reflete imediatamente via optimistic update"
  artifacts:
    - path: "package.json (modificado)"
      provides: "Adiciona dependencia fractional-indexing ^3.2.0"
    - path: "src/utils/lexorank.ts"
      provides: "Wrappers em volta de fractional-indexing: nextKeyAfter, keyBetween, firstKey"
    - path: "src/hooks/useCrudMutations.ts (modificado)"
      provides: "useReorderPage({ pageId, newSortOrder, newParentId? }) mutation com optimistic update"
    - path: "src/components/PageTreeItem.tsx (modificado)"
      provides: "useSortable do dnd-kit + drop zones (entre, dentro), visual feedback"
    - path: "src/components/AppSidebar.tsx (modificado)"
      provides: "DndContext envolvendo a arvore (em adicao ao DndContext de workspaces ja existente)"
  key_links:
    - from: "drag end event"
      to: "useReorderPage.mutateAsync({ pageId, newSortOrder, newParentId })"
      via: "lexorank.keyBetween(prev.sort_order, next.sort_order)"
    - from: "useReorderPage onSuccess"
      to: "invalidate pages-tree[workspace, oldParent] + pages-tree[workspace, newParent]"
      via: "qc.invalidateQueries"
---

# Fase 02 Plano 02-03: Drag/drop na arvore com lexorank

**Objetivo:** Permitir reordenar pages entre irmaos e reaninhar (drop sobre outra page = vira filho) na arvore do sidebar. Usar `fractional-indexing` (lexorank base-62) pra `pages.sort_order`. Restrito ao mesmo workspace. Drop em database e rejeitado.

## Pesquisa inline aplicada

- **fractional-indexing API:** `generateKeyBetween(a, b)` recebe duas strings (ou null) e retorna nova chave entre elas. `null, null` = primeira chave (`a0`). `null, a0` = antes (`Zz`). `a0, null` = depois (`a1`). Para insercao entre A e B existentes: `generateKeyBetween(A, B)`. Charset BASE_62.
- **dnd-kit sortable nested:** Padrao Sortable Tree usa `SortableContext` por nivel + `useDroppable` em containers de filhos. Para o MVP, alternativa mais simples: tratar todas as pages do workspace como uma lista plana ordenada por (parent_id, sort_order) e detectar drop indicator zones (entre vs dentro). Plano usa essa abordagem simplificada.
- **Optimistic update:** Mutation atualiza cache via `qc.setQueryData(['pages-tree', ws, parentId], ...)` antes do server confirm. Em caso de erro, restora.

## Contexto

@src/components/AppSidebar.tsx - DndContext de workspaces ja existe (linhas 546-572 com `verticalListSortingStrategy`). NOSSO DndContext novo sera ESCOPADO ao nivel de pages (dentro de WorkspaceRootPages e PageTreeItem children).
@src/components/PageTreeItem.tsx - receber sortable props
@src/hooks/useCrudMutations.ts - adicionar useReorderPage
@src/types/page.ts - PageTreeNode.sort_order

## Tarefas

<task id="1" type="auto">
<files>package.json</files>
<action>
Adicionar dependencia `fractional-indexing@^3.2.0` em `package.json`:

```bash
npm install fractional-indexing@^3.2.0
```

Confirmar que entrou em `dependencies` (nao `devDependencies`).
</action>
<verify>
  <automated>grep "fractional-indexing" package.json</automated>
</verify>
<done>Dependencia presente. `npm install` rodado com sucesso.</done>
</task>

<task id="2" type="auto">
<files>src/utils/lexorank.ts</files>
<action>
Criar `src/utils/lexorank.ts`:

```typescript
import { generateKeyBetween, generateNKeysBetween } from 'fractional-indexing';

/**
 * Wrappers em volta de fractional-indexing para `pages.sort_order`.
 *
 * Charset BASE_62. Chaves comparaveis via string-compare nativo (NUNCA localeCompare).
 *
 * Convencoes:
 *  - null em a = "antes de tudo"
 *  - null em b = "depois de tudo"
 *  - Para nova page no final de um nivel: nextKeyAfter(last?.sort_order ?? null)
 *  - Para nova page entre A e B: keyBetween(A.sort_order, B.sort_order)
 *  - Para primeira page do mundo: firstKey() === 'a0'
 */

export function firstKey(): string {
  return generateKeyBetween(null, null);
}

export function nextKeyAfter(prev: string | null): string {
  return generateKeyBetween(prev, null);
}

export function previousKeyBefore(next: string | null): string {
  return generateKeyBetween(null, next);
}

export function keyBetween(prev: string | null, next: string | null): string {
  return generateKeyBetween(prev, next);
}

export function nKeysBetween(prev: string | null, next: string | null, n: number): string[] {
  return generateNKeysBetween(prev, next, n);
}

/**
 * Compara duas chaves lexorank (string-compare nativo).
 * Retorna -1, 0, ou 1.
 */
export function compareKeys(a: string, b: string): -1 | 0 | 1 {
  if (a < b) return -1;
  if (a > b) return 1;
  return 0;
}
```
</action>
<verify>
  <automated>test -f src/utils/lexorank.ts && grep -E "keyBetween|nextKeyAfter|firstKey|nKeysBetween" src/utils/lexorank.ts | wc -l</automated>
</verify>
<done>Arquivo criado. Grep retorna >= 4 matches.</done>
</task>

<task id="3" type="auto">
<files>src/hooks/useCrudMutations.ts</files>
<action>
Adicionar mutation `useReorderPage` em `src/hooks/useCrudMutations.ts` (apos `useRenamePage`):

```typescript
/**
 * Reordena page na arvore. Atualiza sort_order e opcionalmente parent_id.
 *
 * @param pageId - id da page
 * @param newSortOrder - chave lexorank base-62 (gerada via @/utils/lexorank.keyBetween)
 * @param newParentId - se mudou de pai; undefined = nao mudou; null = move pra raiz; string = move pra sub
 *
 * Optimistic update: atualiza cache de ['pages-tree', workspaceId, oldParentId|newParentId]
 * antes do server confirm.
 */
export const useReorderPage = () => {
  const qc = useQueryClient();
  return useMutation({
    retry: 1,
    mutationFn: async ({
      pageId,
      newSortOrder,
      newParentId,
    }: {
      pageId: string;
      newSortOrder: string;
      newParentId?: string | null;
    }) => {
      const update: { sort_order: string; parent_id?: string | null } = { sort_order: newSortOrder };
      if (newParentId !== undefined) update.parent_id = newParentId;
      const { data, error } = await supabase
        .from('pages')
        .update(update)
        .eq('id', pageId)
        .select()
        .single();
      if (error) throw error;
      return data;
    },
    onSuccess: () => {
      // Realtime + invalidacao explicita
      qc.invalidateQueries({ queryKey: ['pages-tree'] });
      qc.invalidateQueries({ queryKey: ['pages'] });
      qc.invalidateQueries({ queryKey: ['all-pages'] });
    },
    onError: (error: Error) => {
      toast.error('Erro ao reordenar pagina. Tente novamente.');
      console.error('useReorderPage', error);
    },
  });
};
```
</action>
<verify>
  <automated>grep -E "useReorderPage" src/hooks/useCrudMutations.ts | wc -l</automated>
</verify>
<done>Grep retorna >= 2 matches. `npm run build` passa.</done>
</task>

<task id="4" type="auto">
<files>src/components/PageTreeItem.tsx</files>
<action>
Estender `PageTreeItem.tsx` para suportar drag/drop com dnd-kit:

1. Imports adicionais:
```typescript
import { useSortable, useDroppable } from '@dnd-kit/sortable';
import { CSS } from '@dnd-kit/utilities';
import { useReorderPage } from '@/hooks/useCrudMutations';
import { keyBetween } from '@/utils/lexorank';
```

2. Dentro do componente PageTreeItem:
```typescript
const { attributes, listeners, setNodeRef, transform, transition, isDragging } = useSortable({
  id: `page-${node.id}`,
  data: { type: 'page', pageId: node.id, parentId: node.parent_id, workspaceId },
});

// Drop zone "dentro" deste no (vira filho)
const { setNodeRef: setDropInsideRef, isOver: isOverInside } = useDroppable({
  id: `page-${node.id}-inside`,
  data: { type: 'page-inside', parentId: node.id, workspaceId },
});

const sortableStyle = {
  transform: CSS.Transform.toString(transform),
  transition,
  opacity: isDragging ? 0.5 : 1,
};
```

3. Envolver o wrapper externo com `setNodeRef`, `style={sortableStyle}` e o handle `<GripVertical>` com `{...attributes} {...listeners}` (icone aparece on hover, similar ao SortableWorkspaceItem).

4. Adicionar borda visual quando `isOverInside`: classe condicional `bg-primary/10 ring-1 ring-primary` no container interno (drop "dentro").

5. Drop "entre" e gerenciado pelo SortableContext do parent (task 5).

Imports adicionais: `GripVertical` do lucide-react.

Manter toda a logica existente de PageTreeItem (expand/collapse, dropdown, rename, delete).
</action>
<verify>
  <automated>grep -E "useSortable|useDroppable|useReorderPage|keyBetween" src/components/PageTreeItem.tsx | wc -l</automated>
</verify>
<done>Grep retorna >= 4 matches.</done>
</task>

<task id="5" type="auto">
<files>src/components/AppSidebar.tsx</files>
<action>
Adicionar DndContext escopado a arvore de pages dentro de `WorkspaceItem` (separado do DndContext de workspaces que ja existe em SidebarContent).

1. Imports adicionais:
```typescript
import { DndContext, closestCenter, PointerSensor, useSensor, useSensors, DragOverlay, type DragEndEvent, type DragOverEvent } from '@dnd-kit/core';
import { SortableContext, verticalListSortingStrategy } from '@dnd-kit/sortable';
import { useReorderPage } from '@/hooks/useCrudMutations';
import { keyBetween } from '@/utils/lexorank';
import { useQueryClient } from '@tanstack/react-query';
import type { PageTreeNode } from '@/types/page';
```

2. Modificar `WorkspaceRootPages` (criado em 02-02) para incluir DndContext + SortableContext:

```tsx
const WorkspaceRootPages: React.FC<{
  workspaceId: string;
  expanded: boolean;
  searchQuery?: string;
}> = ({ workspaceId, expanded, searchQuery }) => {
  const qc = useQueryClient();
  const { data: rootPages = [] } = usePagesTree(workspaceId, null, expanded);
  const reorderPage = useReorderPage();

  const sensors = useSensors(
    useSensor(PointerSensor, { activationConstraint: { distance: 8 } })
  );

  const handleDragEnd = async (event: DragEndEvent) => {
    const { active, over } = event;
    if (!over || active.id === over.id) return;

    const activeData = active.data.current as { type: 'page'; pageId: string; parentId: string | null; workspaceId: string };
    const overData = over.data.current as { type: 'page' | 'page-inside'; pageId?: string; parentId?: string | null; workspaceId: string };

    // Validacao: rejeitar drop entre workspaces
    if (activeData.workspaceId !== overData.workspaceId) {
      toast.error('Nao e possivel mover pagina entre workspaces');
      return;
    }

    // Rejeitar drop em database (id comeca com 'db-')
    if (typeof over.id === 'string' && over.id.startsWith('db-')) {
      toast.error('Nao e possivel aninhar pagina dentro de database');
      return;
    }

    let newParentId: string | null | undefined;
    let prevKey: string | null = null;
    let nextKey: string | null = null;

    if (overData.type === 'page-inside') {
      // Drop "dentro" de outra page: vira filho
      newParentId = overData.parentId ?? null;
      // Buscar ultimo filho desse novo parent para colocar no final
      const siblings = (qc.getQueryData<PageTreeNode[]>(['pages-tree', workspaceId, newParentId]) ?? []);
      prevKey = siblings.length > 0 ? siblings[siblings.length - 1].sort_order : null;
      nextKey = null;
    } else {
      // Drop "entre": mesmo parent, posicao entre vizinhos
      newParentId = overData.parentId ?? null;
      const siblings = (qc.getQueryData<PageTreeNode[]>(['pages-tree', workspaceId, newParentId]) ?? []);
      const overIdx = siblings.findIndex((p) => `page-${p.id}` === over.id);
      if (overIdx === -1) return;
      // Determina lado do drop pelo delta vertical (heuristica simples: sempre coloca apos overIdx)
      prevKey = siblings[overIdx]?.sort_order ?? null;
      nextKey = siblings[overIdx + 1]?.sort_order ?? null;
    }

    const newSortOrder = keyBetween(prevKey, nextKey);
    try {
      await reorderPage.mutateAsync({
        pageId: activeData.pageId,
        newSortOrder,
        newParentId: activeData.parentId === newParentId ? undefined : newParentId,
      });
    } catch { /* erro toasted */ }
  };

  if (rootPages.length === 0) return null;

  return (
    <DndContext sensors={sensors} collisionDetection={closestCenter} onDragEnd={handleDragEnd}>
      <SortableContext
        items={rootPages.map((p) => `page-${p.id}`)}
        strategy={verticalListSortingStrategy}
      >
        <div className="density-space-y">
          {rootPages.map((page) => (
            <PageTreeItem
              key={`page-${page.id}`}
              node={page}
              workspaceId={workspaceId}
              level={0}
              searchQuery={searchQuery}
            />
          ))}
        </div>
      </SortableContext>
    </DndContext>
  );
};
```

3. Importante: o DndContext de workspaces ja existente em `SidebarContent` continua funcionando independente. dnd-kit suporta multiplos DndContexts aninhados sem conflito (cada um tem seu proprio onDragEnd).

4. Para drops "dentro" nos nos filhos (subpages), o SortableContext + dropzones de PageTreeItem (criados em task 4) lidam com isso recursivamente. Cada PageTreeItem expandido envolve seus filhos em mais um SortableContext aninhado se necessario (alternativa: usar UM SortableContext flat com todos os ids da arvore visivel - mais simples, fica como decisao na implementacao).
</action>
<verify>
  <automated>grep -E "DndContext|SortableContext|handleDragEnd|reorderPage" src/components/AppSidebar.tsx | wc -l</automated>
</verify>
<done>Grep retorna >= 6 matches. `npm run build` passa. Drag/drop visual funciona (handle aparece on hover).</done>
</task>

<task id="6" type="auto">
<files>src/test/lexorank.test.ts</files>
<action>
Criar teste unitario `src/test/lexorank.test.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import { firstKey, nextKeyAfter, previousKeyBefore, keyBetween, compareKeys, nKeysBetween } from '@/utils/lexorank';

describe('lexorank utils', () => {
  it('firstKey retorna a0', () => {
    expect(firstKey()).toBe('a0');
  });

  it('nextKeyAfter gera chaves crescentes', () => {
    const k1 = firstKey();
    const k2 = nextKeyAfter(k1);
    const k3 = nextKeyAfter(k2);
    expect(k1 < k2).toBe(true);
    expect(k2 < k3).toBe(true);
  });

  it('previousKeyBefore gera chave menor', () => {
    const k1 = firstKey();
    const before = previousKeyBefore(k1);
    expect(before < k1).toBe(true);
  });

  it('keyBetween gera chave entre dois pontos', () => {
    const k1 = firstKey();      // a0
    const k2 = nextKeyAfter(k1); // a1
    const between = keyBetween(k1, k2);
    expect(between > k1).toBe(true);
    expect(between < k2).toBe(true);
  });

  it('compareKeys retorna -1/0/1', () => {
    expect(compareKeys('a0', 'a1')).toBe(-1);
    expect(compareKeys('a1', 'a1')).toBe(0);
    expect(compareKeys('a1', 'a0')).toBe(1);
  });

  it('nKeysBetween retorna N chaves distintas crescentes', () => {
    const keys = nKeysBetween(null, null, 5);
    expect(keys).toHaveLength(5);
    for (let i = 1; i < keys.length; i++) {
      expect(keys[i] > keys[i - 1]).toBe(true);
    }
  });
});
```
</action>
<verify>
  <automated>npx vitest run src/test/lexorank.test.ts 2>&1 | tail -5</automated>
</verify>
<done>Os 6 testes passam.</done>
</task>

## Criterios de Sucesso

- [ ] `fractional-indexing@^3.2.0` instalado
- [ ] `src/utils/lexorank.ts` exporta firstKey, nextKeyAfter, previousKeyBefore, keyBetween, compareKeys, nKeysBetween
- [ ] `useReorderPage` mutation atualiza sort_order (+ opcionalmente parent_id)
- [ ] PageTreeItem usa useSortable + useDroppable (zona "dentro")
- [ ] Drop entre irmaos: keyBetween(prev, next) -> update sort_order
- [ ] Drop sobre page (zona "dentro"): vira filho dessa page (parent_id atualizado)
- [ ] Drop em database: rejeitado com toast.error
- [ ] Drop entre workspaces: rejeitado com toast.error
- [ ] `npm run test` passa (incluindo 6 novos testes de lexorank)
- [ ] `npm run build` passa
