---
phase: 02-notion-database-hierarquia-blocos-extras
plan: 02-02
type: feature
autonomous: true
wave: 1
depends_on: [02-01]
requirements: [REQ-10, REQ-11]
must_haves:
  truths:
    - "Sidebar lista pages como arvore expansivel: workspace > page > (subpages | databases)"
    - "Cada no carrega filhos lazy ao expandir (sem fetch eager da arvore inteira)"
    - "Pages tem icone FileText, databases (boards com page_id) tem icone Database; indentacao 16px por nivel"
    - "Estado de expansao persiste em localStorage com prefix lfpro-"
    - "Buscar no sidebar funciona em qualquer nivel da arvore"
  artifacts:
    - path: "src/hooks/usePagesTree.ts"
      provides: "Hook usePagesTree(workspaceId, parentId) que retorna { children: PageTreeNode[], databases: Board[], isLoading } com lazy load por nivel"
    - path: "src/components/PageTreeItem.tsx"
      provides: "Componente recursivo de no da arvore (page ou subpage), com chevron expand/collapse, click navega, dropdown de acoes"
    - path: "src/components/DatabaseSidebarItem.tsx"
      provides: "Item de database (board com page_id) no sidebar - icone Database, click navega pra /page/:parentPageId#db=:boardId ou abre direto a database"
    - path: "src/components/AppSidebar.tsx (modificado)"
      provides: "Renderiza PageTreeItem recursivo dentro do workspace, substituindo lista plana atual de pages"
  key_links:
    - from: "PageTreeItem"
      to: "usePagesTree(workspaceId, page.id)"
      via: "lazy fetch ao expandir"
    - from: "AppSidebar WorkspaceItem"
      to: "PageTreeItem (root level)"
      via: "usePagesTree(workspaceId, null) na montagem"
    - from: "DatabaseSidebarItem"
      to: "/page/:parentPageId (ou rota nova)"
      via: "react-router navigate"
---

# Fase 02 Plano 02-02: Sidebar tree expansivel

**Objetivo:** Substituir a lista plana de pages no sidebar por arvore expansivel com lazy load por nivel. Cada page pode ter subpages (de `pages.parent_id`) e databases (`boards` com `page_id` apontando pra essa page). Drag/drop fica para Plano 02-03.

## Pesquisa inline aplicada

- **Lazy load por nivel:** Query `usePagesTree(workspaceId, parentId)` com key `['pages-tree', workspaceId, parentId]` so dispara quando o no esta expandido. React Query mantem cache por nivel.
- **child_count via subquery:** Em vez de carregar todos os filhos pra mostrar chevron, calculamos `child_count` no servidor (subquery COUNT em pages com `parent_id = p.id` + COUNT em boards com `page_id = p.id`). Evita fetch redundante.
- **TanStack Virtual:** NAO usar nesta fase. Sidebar com 1000+ pages e edge case adiado (CONTEXT.md fala em adiar virtualizacao agressiva). Manter render simples.

## Contexto

@src/components/AppSidebar.tsx - sidebar atual com WorkspaceItem (linhas 81-297). Lista pages plana entre linhas 247-261. Modificar SOMENTE essa secao.
@src/components/PageSidebarItem.tsx - modelo de item visual (sera referencia, nao reusado direto porque PageTreeItem e recursivo)
@src/hooks/useSupabaseData.ts - adicionar usePagesTree, useDatabasesForPage
@src/types/page.ts (apos 02-01) - PageTreeNode com child_count

## Tarefas

<task id="1" type="auto">
<files>src/hooks/useSupabaseData.ts</files>
<action>
Adicionar 2 hooks novos em `src/hooks/useSupabaseData.ts` (apos `useWorkspaceEntries`):

```typescript
import type { PageTreeNode } from '@/types/page';

/**
 * Retorna filhos de um no da arvore (pages com parent_id = parentId).
 * Quando parentId e null, retorna pages raiz do workspace (parent_id IS NULL).
 * Inclui child_count via subquery pra renderizar chevron sem fetch redundante.
 *
 * Ordenacao por sort_order (lexorank ASC).
 *
 * Lazy: queryFn so roda quando `enabled` (consumer controla via expanded state).
 */
export const usePagesTree = (
  workspaceId: string | null | undefined,
  parentId: string | null,
  enabled: boolean = true,
) =>
  useQuery({
    queryKey: ['pages-tree', workspaceId, parentId],
    enabled: !!workspaceId && enabled,
    staleTime: 2 * 60 * 1000,
    queryFn: async () => {
      // Query principal: pages filhos diretos
      let q = supabase
        .from('pages')
        .select('id, workspace_id, parent_id, title, icon, sort_order')
        .eq('workspace_id', workspaceId!)
        .eq('state', 'active');
      q = parentId === null ? q.is('parent_id', null) : q.eq('parent_id', parentId);
      const { data: pages, error } = await q.order('sort_order');
      if (error) throw error;
      if (!pages || pages.length === 0) return [] as PageTreeNode[];

      const pageIds = pages.map((p) => p.id);

      // Subquery 1: count de subpages por parent_id
      const { data: subpageCounts } = await supabase
        .from('pages')
        .select('parent_id')
        .in('parent_id', pageIds)
        .eq('state', 'active');

      // Subquery 2: count de databases (boards.page_id IN pageIds) por page_id
      const { data: databases } = await supabase
        .from('boards')
        .select('page_id')
        .in('page_id', pageIds)
        .eq('state', 'active');

      // Agregar contagens em memoria (datasets pequenos por nivel)
      const childCount: Record<string, number> = {};
      for (const row of subpageCounts ?? []) {
        const pid = (row as { parent_id: string | null }).parent_id;
        if (pid) childCount[pid] = (childCount[pid] ?? 0) + 1;
      }
      for (const row of databases ?? []) {
        const pid = (row as { page_id: string | null }).page_id;
        if (pid) childCount[pid] = (childCount[pid] ?? 0) + 1;
      }

      return pages.map<PageTreeNode>((p) => ({
        id: p.id,
        workspace_id: p.workspace_id!,
        parent_id: p.parent_id,
        title: p.title,
        icon: p.icon,
        sort_order: p.sort_order,
        child_count: childCount[p.id] ?? 0,
      }));
    },
  });

/**
 * Retorna boards que sao databases inline ancoradas numa page (boards.page_id = pageId).
 * Usado pelo PageTreeItem para listar databases como filhos da page.
 */
export const useDatabasesForPage = (pageId: string | null | undefined, enabled: boolean = true) =>
  useQuery({
    queryKey: ['databases-for-page', pageId],
    enabled: !!pageId && enabled,
    staleTime: 2 * 60 * 1000,
    queryFn: async () => {
      const { data, error } = await supabase
        .from('boards')
        .select('id, name, icon, color, page_id, workspace_id, position')
        .eq('page_id', pageId!)
        .eq('state', 'active')
        .order('position')
        .order('created_at');
      if (error) throw error;
      return data ?? [];
    },
  });
```

Importar `PageTreeNode` no topo do arquivo. Adicionar `as any` em `supabase.from('pages').select(...).eq('parent_id', ...)` SE o tipo gerado ainda nao tem parent_id (depende de 02-01 ter sido aplicado em types.ts - provavelmente nao precisa porque task 4 do 02-01 estende types.ts).
</action>
<verify>
  <automated>grep -E "usePagesTree|useDatabasesForPage" src/hooks/useSupabaseData.ts | wc -l</automated>
</verify>
<done>Grep retorna >= 4 matches. `npm run build` passa.</done>
</task>

<task id="2" type="auto">
<files>src/components/DatabaseSidebarItem.tsx</files>
<action>
Criar componente novo `src/components/DatabaseSidebarItem.tsx`:

```typescript
import React from 'react';
import { useNavigate, useLocation } from 'react-router-dom';
import { Database } from 'lucide-react';

interface DatabaseSidebarItemProps {
  databaseId: string;       // boards.id
  parentPageId: string;     // boards.page_id
  name: string;
  icon: string | null;      // se vier emoji, override do Lucide
  color: string | null;     // pra dot opcional
  level: number;            // indentacao 16px * level
}

/**
 * Renderiza database inline (board com page_id) no sidebar como filho da page.
 * Click navega pra /page/:parentPageId e foca no bloco database (anchor #db=:databaseId).
 *
 * Visual: icone Database (Lucide) ou emoji se icon estiver setado.
 * Indentacao 16px por nivel.
 */
const DatabaseSidebarItem: React.FC<DatabaseSidebarItemProps> = ({
  databaseId, parentPageId, name, icon, color, level,
}) => {
  const navigate = useNavigate();
  const location = useLocation();
  const isActive = location.pathname === `/page/${parentPageId}` && location.hash === `#db=${databaseId}`;

  return (
    <button
      onClick={() => navigate(`/page/${parentPageId}#db=${databaseId}`)}
      className={`flex items-center w-full density-px density-py-item text-sm rounded-md transition-colors duration-[70ms] ${
        isActive
          ? 'bg-primary/15 text-primary font-semibold'
          : 'text-muted-foreground hover:bg-sidebar-accent hover:text-sidebar-foreground'
      }`}
      style={{ paddingLeft: `${level * 16 + 8}px` }}
      title={name}
    >
      <span className="flex-shrink-0 mr-2 inline-flex items-center justify-center w-3.5">
        {icon
          ? <span className="text-sm leading-none">{icon}</span>
          : <Database className="w-3.5 h-3.5" style={color ? { color } : undefined} />}
      </span>
      <span className="font-density-cell truncate flex-1 text-left">{name}</span>
    </button>
  );
};

export default DatabaseSidebarItem;
```
</action>
<verify>
  <automated>test -f src/components/DatabaseSidebarItem.tsx && grep -c "Database\b" src/components/DatabaseSidebarItem.tsx</automated>
</verify>
<done>Arquivo criado. Grep do icon Database retorna >= 2 (import + uso). Sem em-dash.</done>
</task>

<task id="3" type="auto">
<files>src/components/PageTreeItem.tsx</files>
<action>
Criar componente recursivo `src/components/PageTreeItem.tsx`:

```typescript
import React, { useState } from 'react';
import { useNavigate, useLocation } from 'react-router-dom';
import { ChevronDown, ChevronRight, FileText, MoreHorizontal, Pencil, Trash2, Plus } from 'lucide-react';
import {
  DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuTrigger, DropdownMenuSeparator,
} from '@/components/ui/dropdown-menu';
import {
  AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent,
  AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle,
} from '@/components/ui/alert-dialog';
import { useDeletePage, useRenamePage, useCreatePage } from '@/hooks/useCrudMutations';
import { usePagesTree, useDatabasesForPage } from '@/hooks/useSupabaseData';
import DatabaseSidebarItem from '@/components/DatabaseSidebarItem';
import { toast } from 'sonner';
import type { PageTreeNode } from '@/types/page';

interface PageTreeItemProps {
  node: PageTreeNode;
  workspaceId: string;
  level: number; // 0 = raiz do workspace, +1 a cada subnivel
  searchQuery?: string; // filtro pra esconder nos que nao batem
}

/**
 * No recursivo da arvore de pages no sidebar.
 *
 * Comportamento:
 *  - Click no titulo navega pra /page/:id
 *  - Click no chevron expande/colapsa (so aparece se child_count > 0)
 *  - Ao expandir, dispara usePagesTree (subpages) + useDatabasesForPage (databases inline)
 *  - Estado de expansao persiste em localStorage `lfpro-page-expanded-:id`
 *  - Indentacao 16px * level
 *  - Acoes no dropdown: Renomear, Nova subpagina, Excluir
 */
const PageTreeItem: React.FC<PageTreeItemProps> = ({ node, workspaceId, level, searchQuery }) => {
  const navigate = useNavigate();
  const location = useLocation();
  const isActive = location.pathname === `/page/${node.id}`;
  const storageKey = `lfpro-page-expanded-${node.id}`;

  const [expanded, setExpandedState] = useState<boolean>(() => {
    try { return localStorage.getItem(storageKey) === 'true'; } catch { return false; }
  });
  const setExpanded = (v: boolean) => {
    setExpandedState(v);
    try { localStorage.setItem(storageKey, String(v)); } catch {}
  };

  const [renaming, setRenaming] = useState(false);
  const [renameValue, setRenameValue] = useState(node.title);
  const [showDelete, setShowDelete] = useState(false);

  const deletePage = useDeletePage();
  const renamePage = useRenamePage();
  const createPage = useCreatePage();

  // Lazy: so busca filhos quando expandido
  const { data: childPages = [] } = usePagesTree(workspaceId, node.id, expanded);
  const { data: childDatabases = [] } = useDatabasesForPage(node.id, expanded);

  const hasChildren = node.child_count > 0;

  // Filtrar por busca (so escondemos folhas; nos com filhos que batem continuam visiveis)
  const matchesSearch = !searchQuery || node.title.toLowerCase().includes(searchQuery.toLowerCase());

  const handleRename = async () => {
    const next = renameValue.trim();
    if (!next || next === node.title) { setRenaming(false); setRenameValue(node.title); return; }
    try { await renamePage.mutateAsync({ pageId: node.id, title: next }); toast.success('Pagina renomeada'); }
    catch { /* erro toasted pela mutation */ }
    setRenaming(false);
  };

  const handleDelete = async () => {
    try {
      await deletePage.mutateAsync(node.id);
      toast.success('Pagina excluida');
      setShowDelete(false);
    } catch { /* erro toasted */ }
  };

  const handleCreateSubpage = async () => {
    try {
      // useCreatePage atual nao aceita parent_id; precisamos estender (Plano 02-04)
      // Por ora, criamos via dispatch event pra 02-04 escutar e abrir modal.
      window.dispatchEvent(new CustomEvent('lfpro-create-subpage', {
        detail: { workspaceId, parentId: node.id },
      }));
      setExpanded(true);
    } catch { /* */ }
  };

  if (!matchesSearch && childPages.length === 0 && childDatabases.length === 0) return null;

  return (
    <>
      <div className="flex items-center group/page" style={{ paddingLeft: `${level * 16}px` }}>
        {hasChildren ? (
          <button
            onClick={() => setExpanded(!expanded)}
            className="p-0.5 rounded text-muted-foreground hover:bg-sidebar-accent"
            aria-label={expanded ? 'Recolher' : 'Expandir'}
          >
            {expanded ? <ChevronDown className="w-3 h-3" /> : <ChevronRight className="w-3 h-3" />}
          </button>
        ) : (
          <span className="w-4" />
        )}
        {renaming ? (
          <input
            value={renameValue}
            onChange={(e) => setRenameValue(e.target.value)}
            ref={(el) => el?.focus({ preventScroll: true })}
            onBlur={handleRename}
            onKeyDown={(e) => {
              if (e.key === 'Enter') handleRename();
              if (e.key === 'Escape') { setRenaming(false); setRenameValue(node.title); }
            }}
            className="flex-1 min-w-0 density-px density-py text-xs bg-muted rounded-md outline-none border border-primary mx-1"
          />
        ) : (
          <button
            onClick={() => navigate(`/page/${node.id}`)}
            className={`flex items-center flex-1 density-px density-py-item text-sm rounded-md transition-colors duration-[70ms] ${
              isActive
                ? 'bg-primary/15 text-primary font-semibold'
                : 'text-muted-foreground hover:bg-sidebar-accent hover:text-sidebar-foreground'
            }`}
          >
            <span className="flex-shrink-0 mr-2 inline-flex items-center justify-center w-3.5">
              {node.icon
                ? <span className="text-sm leading-none">{node.icon}</span>
                : <FileText className="w-3.5 h-3.5" />}
            </span>
            <span className="font-density-cell truncate flex-1 text-left">{node.title}</span>
          </button>
        )}
        <DropdownMenu>
          <DropdownMenuTrigger asChild>
            <button
              className="p-1 rounded hover:bg-sidebar-accent text-muted-foreground opacity-0 group-hover/page:opacity-100 transition-all mr-0.5"
              aria-label="Opcoes da pagina"
            >
              <MoreHorizontal className="w-3 h-3" />
            </button>
          </DropdownMenuTrigger>
          <DropdownMenuContent align="end" className="w-44">
            <DropdownMenuItem onClick={handleCreateSubpage}>
              <Plus className="w-3.5 h-3.5 mr-2" /> Nova subpagina
            </DropdownMenuItem>
            <DropdownMenuItem onClick={() => { setRenameValue(node.title); setRenaming(true); }}>
              <Pencil className="w-3.5 h-3.5 mr-2" /> Renomear
            </DropdownMenuItem>
            <DropdownMenuSeparator />
            <DropdownMenuItem onClick={() => setShowDelete(true)} className="text-destructive focus:text-destructive">
              <Trash2 className="w-3.5 h-3.5 mr-2" /> Excluir
            </DropdownMenuItem>
          </DropdownMenuContent>
        </DropdownMenu>
      </div>

      {/* Expansao animada */}
      {expanded && hasChildren && (
        <div className="density-space-y">
          {childPages.map((child) => (
            <PageTreeItem
              key={`page-${child.id}`}
              node={child}
              workspaceId={workspaceId}
              level={level + 1}
              searchQuery={searchQuery}
            />
          ))}
          {childDatabases.map((db: { id: string; name: string; icon: string | null; color: string | null }) => (
            <DatabaseSidebarItem
              key={`db-${db.id}`}
              databaseId={db.id}
              parentPageId={node.id}
              name={db.name}
              icon={db.icon}
              color={db.color}
              level={level + 1}
            />
          ))}
        </div>
      )}

      <AlertDialog open={showDelete} onOpenChange={setShowDelete}>
        <AlertDialogContent>
          <AlertDialogHeader>
            <AlertDialogTitle>Excluir pagina?</AlertDialogTitle>
            <AlertDialogDescription>
              A pagina &quot;{node.title}&quot; e todas as subpaginas e databases dentro dela serao movidas para a lixeira.
            </AlertDialogDescription>
          </AlertDialogHeader>
          <AlertDialogFooter>
            <AlertDialogCancel>Cancelar</AlertDialogCancel>
            <AlertDialogAction
              onClick={handleDelete}
              className="bg-destructive text-destructive-foreground hover:bg-destructive/90"
            >
              Excluir
            </AlertDialogAction>
          </AlertDialogFooter>
        </AlertDialogContent>
      </AlertDialog>
    </>
  );
};

export default PageTreeItem;
```
</action>
<verify>
  <automated>test -f src/components/PageTreeItem.tsx && grep -E "PageTreeItem|usePagesTree|useDatabasesForPage|lfpro-page-expanded" src/components/PageTreeItem.tsx | wc -l</automated>
</verify>
<done>Arquivo criado. Grep retorna >= 4 matches. Sem em-dash. Sem `any`.</done>
</task>

<task id="4" type="auto">
<files>src/components/AppSidebar.tsx</files>
<action>
Modificar `src/components/AppSidebar.tsx` no componente `WorkspaceItem` (linhas 81-297):

1. Remover import nao usado de `WorkspaceEntry` se PageTreeItem assumir o papel.
2. Adicionar import: `import PageTreeItem from '@/components/PageTreeItem';` e `import { usePagesTree } from '@/hooks/useSupabaseData';`
3. Dentro de `WorkspaceItem`, substituir o bloco de pages plano (atualmente entre comentario "Pages do workspace (lista direta, sem folder no MVP)" e o `<CreateBoardModal>`) por:

```tsx
{/* Arvore de pages root do workspace (recursiva via PageTreeItem) */}
<WorkspaceRootPages
  workspaceId={workspace.id}
  expanded={expanded}
  searchQuery={searchQuery || sidebarSearch}
/>
```

E criar um helper interno antes de `WorkspaceItem`:

```tsx
const WorkspaceRootPages: React.FC<{
  workspaceId: string;
  expanded: boolean;
  searchQuery?: string;
}> = ({ workspaceId, expanded, searchQuery }) => {
  const { data: rootPages = [] } = usePagesTree(workspaceId, null, expanded);
  if (rootPages.length === 0) return null;
  return (
    <div className="density-space-y">
      {rootPages.map((page) => (
        <PageTreeItem
          key={`page-${page.id}`}
          node={page}
          workspaceId={workspaceId}
          level={0}
          searchQuery={searchQuery}
        />
      ))}
    </div>
  );
};
```

Remover o uso de `useWorkspaceEntries(workspace.id)` no `WorkspaceItem` se ele so servia pra listar pages (manter se ainda usa pra boards - checar grep antes). Boards continuam vindo via `workspace.boards` (props) renderizados por `WorkspaceFolders`.

Garantir que o filtro de busca atual em `WorkspaceItem` continua funcionando: `filteredPages` pode ser removido em favor do `searchQuery` propagado pra `PageTreeItem`.
</action>
<verify>
  <automated>grep -E "PageTreeItem|WorkspaceRootPages|usePagesTree" src/components/AppSidebar.tsx | wc -l</automated>
</verify>
<done>Grep retorna >= 3 matches. `npm run build` passa. AppSidebar nao quebra render existente (boards ainda aparecem via WorkspaceFolders).</done>
</task>

<task id="5" type="auto">
<files>src/hooks/useRealtimeSync.ts</files>
<action>
Adicionar invalidacao de `pages-tree` e `databases-for-page` no listener de `pages` e `boards` em `src/hooks/useRealtimeSync.ts`:

```typescript
.on('postgres_changes', { event: '*', schema: 'public', table: 'pages' }, (payload) => {
  const pageId = (payload.new as any)?.id || (payload.old as any)?.id;
  const parentId = (payload.new as any)?.parent_id ?? (payload.old as any)?.parent_id ?? null;
  const workspaceId = (payload.new as any)?.workspace_id || (payload.old as any)?.workspace_id;
  qc.invalidateQueries({ queryKey: ['pages'] });
  qc.invalidateQueries({ queryKey: ['all-pages'] });
  if (pageId) {
    qc.invalidateQueries({ queryKey: ['page', pageId] });
  }
  // Invalida tree no nivel afetado (parent_id)
  if (workspaceId) {
    qc.invalidateQueries({ queryKey: ['pages-tree', workspaceId, parentId] });
    // Sempre invalida root pois child_count do pai muda
    qc.invalidateQueries({ queryKey: ['pages-tree', workspaceId, null] });
  }
})
```

E no listener de `boards`:
```typescript
.on('postgres_changes', { event: '*', schema: 'public', table: 'boards' }, (payload) => {
  const pageId = (payload.new as any)?.page_id ?? (payload.old as any)?.page_id;
  qc.invalidateQueries({ queryKey: ['boards'] });
  if (pageId) {
    qc.invalidateQueries({ queryKey: ['databases-for-page', pageId] });
    qc.invalidateQueries({ queryKey: ['pages-tree'] }); // child_count pode ter mudado
  }
})
```

Usar Edit para modificar APENAS esses handlers, preservando o resto do arquivo.
</action>
<verify>
  <automated>grep -E "pages-tree|databases-for-page" src/hooks/useRealtimeSync.ts | wc -l</automated>
</verify>
<done>Grep retorna >= 3 matches. `npm run build` passa.</done>
</task>

## Criterios de Sucesso

- [ ] Sidebar mostra pages como arvore (chevron expand/collapse por no com child_count > 0)
- [ ] Expandir page busca subpages + databases lazy (queries `pages-tree` e `databases-for-page`)
- [ ] Indentacao visual 16px por nivel
- [ ] Databases (boards com page_id) aparecem com icone Database; subpages com FileText
- [ ] Estado de expansao persiste em `lfpro-page-expanded-:id`
- [ ] Busca no sidebar (input ja existente) filtra titulos da arvore
- [ ] Realtime: criar/editar/deletar page invalida pages-tree do nivel afetado; criar/deletar database invalida databases-for-page
- [ ] CustomEvent `lfpro-create-subpage` disparado pelo dropdown (consumido em 02-04)
- [ ] `npm run build` passa
- [ ] Compatibilidade: pages sem subpages funcionam (chevron oculto, navegacao normal)
